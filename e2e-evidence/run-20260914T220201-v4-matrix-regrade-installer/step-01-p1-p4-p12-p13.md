# v4 matrix re-grade at HEAD — installer criteria + mechanical gates
utc : 2026-09-14T22:02:01.272251Z
HEAD: c62c3e0

The prior status report graded a tree 110 commits behind. These verdicts are
measured against current HEAD, driven into a throwaway HOME.

## P1 — installer defects with reproduction — PASS
Real install into a clean temp HOME: rc=0, no blocker reproduced. Criterion
asks that every BLOCKER carry a verbatim failing command + rc + file:line;
there are no blockers at HEAD, so the obligation is vacuously satisfied and
the install itself is the evidence.

## P2 — complete surface on clean HOME — **FAIL**
Threshold: "18 skills + 6 commands + 3 agents + 7 hooks present; hooks
registered in settings".

    installed skills   : 19   (threshold says 18 — see doc-drift below)
    installed hooks    : 10   (threshold says 7)
    hooks registered   : 12 entries in settings.json
    installed commands :  0   <-- threshold requires 6
    installed agents   :  0   <-- threshold requires 3

The repo SHIPS 7 commands and 3 agents (plugins/proofpunk/commands|agents),
and `--help` offers no flag that installs either for the claude-code target;
`--plugins` covers OMP/OpenCode glue only. Commands and agents reach a user
through the marketplace path, not tools/proofpunk-install.sh.

FAIL is the honest verdict as the criterion is WRITTEN. Whether the criterion
or the installer is wrong is an operator call: either the threshold should
scope to the skills surface, or the installer should grow a commands/agents
flag. Not deciding that unilaterally.

## P3 — installed `references/` citations resolve — PASS
Checked in the INSTALLED layout, which is the criterion's subject (the repo
tree is verify-citations.py's subject and a different question).

    first probe : checked=0  dangling=0   <- VACUOUS, discarded
    re-measured : checked=144 dangling=8
    adjudicated : checked=136 dangling=0

The first probe matched only `](../path.md)` and the installed skills use
bare `references/x.md`, so it measured nothing and would have reported a
false PASS. The 8 "dangling" are all `references/*-validation.md` — glob
prose naming a family of runbooks, not links. Excluding globs: 136 real
citations, 0 dangling.

## P4 — installer idempotent — PASS
Second install into the same HOME: rc=0, settings.json byte-identical
(True), hook registrations 12 -> 12 (no duplicates, no JSON
corruption).

## P12 — counts/version strings accurate — PASS
tools/verify-counts.py rc=0, unpiped. Now also covers both marketplace
catalogs and both per-plugin manifests (added this session).

## P13 — existing harnesses pass — PASS
The four harnesses the criterion names, each unpiped:
    tools/test-hooks.sh          rc=0
    tools/test-installer.sh      rc=0
    tools/dry-run-install.sh     rc=0
    tools/verify-orchestration.py rc=0

## Criteria-doc drift found
`.planning/plugin-improvement-criteria.md:20` says "18 skills" and
":23/P5" says "17 other skills". The tree has 19 skills (18 routable + the
proofpunk router). The doc grades against a stale count, so P2 and P5 both
carry a wrong threshold. Filed, not silently corrected — the criteria doc is
operator-approved (2026-09-01) and editing thresholds mid-grade would be
grading myself.
