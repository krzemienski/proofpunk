1|{
2|  "verdict": "ADOPT-WITH-CHANGES",
3|  "summary": "C2's strategy (additive probes, preserve existing signal, control-arm discipline, canary) is correct relative to rejected alternatives C1/C3/C4, but the plan in r1-candidates.md/r2-patterns.md is not directly implementable as written. It has one load-bearing, undisambiguated design fork that — read the natural/literal way — makes gauge 4 WORSE (4/8 instead of 4/6), plus two further gaps that force an implementer to guess wrong.",
4|  "critical_finding": "gauge-report.py's g_command_surface_proven() (tools/gauge-report.py:537-577) recomputes total=len(cmds) dynamically (:551) but counts only reached_level=='c' by exact string match (:562), completely ignoring the JSON's own 'full_chain' field. Appending 2 new COMMANDS rows (the literal reading of C2's 'New cmd_slash_install_effects/cmd_slash_verify_effects probes') grows total to 8 while the new rows can never be 'c' (install/verify structurally have skill=None per verify-command-surface.py:68-77), yielding 4/8 UNMET -- a worse ratio than today's 4/6. The correct schema keeps the row count at 6 (install/verify already ARE 2 of the 6 real commands per plugins/proofpunk/commands/*.md) and instead promotes those 2 existing rows' reached_level to a new value (e.g. 'effect-proven') while extending gauge-report.py's predicate to reached_level in ('c','effect-proven'). Neither research doc states this; the naming convention '_effects' actively suggests the wrong (row-appending) implementation.",
5|  "at_risk_signals": [
6|    "verify-command-surface.py:277 ok=(n_plugin_pass==6 and n_control_fail==6 and n_honest_max==6) -- 3 hardcoded ==6 literals; false-FAILs on manual invocation if COMMANDS grows past 6, even though not itself CI-wired (gates.yml:17-21, verify-shipped-vs-active.py:47 explicitly exclude it from CI)",
7|    "docs/v3-gauges.md row 24 -- hand-authored prose/sha256 citation, must be regenerated via gauge-report.py per its own stated authority rule (docs/v3-gauges.md:5-7), not hand-edited",
8|    "verify-mutation-artifact.py's existing PASS link for verify-command-surface.py (stale step-14 log) -- reusing it without a fresh mutation artifact for the NEW logic would satisfy the Class-1 detector's mechanical check without proving anything about the new code"
9|  ],
10|  "underspecified_gaps": [
11|    "Write-tool mechanism unnamed: sdk_probe.py:314-319 says allowed_tools cannot grant a missing tool; _SLASH_PLAYBOOK_TOOLS (sdk_probe.py:190-195) disallows Write/Edit/Bash/Agent. The SEALED evidence already shows cmd_slash_install's plugin arm writing via an AMBIENT mcp__filesystem__write_file MCP tool (command-surface-proof.json:872-994), not the first-party Write tool its own 'why' field claims is unavailable -- neither doc says whether new probes should explicitly grant first-party Write (portable) or depend on this ambient host MCP server (non-hermetic, unasserted)",
12|    "/proofpunk:verify's actual contract (verify.md:6,10 -- run a real server/CLI/UI, cite executed output) is fundamentally different from install's file-merge contract that C2's canary/write design is copied from. A genuine full-chain proof of verify ALREADY EXISTS at evidence/v3-release/l16-commands/verify/RUN.md via headless claude -p + Agent delegation to proofpunk:end-user-validate + real Bash execution + independent byte-count cross-check -- but that used Agent/Task, which _SLASH_PLAYBOOK_TOOLS also disallows, a completely different grant than 'write-enabled'. Neither doc reconciles this.",
13|    "verify-command-surface.py main() shares ONE tempfile.mkdtemp() (:183) across all 6 commands and both arms today -- doesn't satisfy Pattern 3's own 'fresh sandbox per arm' text even for existing passing probes; undecided whether to fix repo-wide or scope narrowly",
14|    "No teardown specified for new per-arm temp dirs",
15|    "No precise byte-match semantics for the install canary given the written file is template-substituted and platform-dependent"
16|  ],
17|  "mutation_proof_recipe": "1) Baseline: new probes PASS including new checks. 2) Named one-line mutation to the new check logic itself (e.g. hardcode checks['canary_survived']=True), matching sdk_probe.py's existing require_write_attempt pattern. 3) Run against a deliberately regressed fixture; unmutated harness must FAIL naming the command via the row's existing command field (verify-command-surface.py:212) -- no new naming mechanism needed. 4) Restore; sha256_of() (already defined :81-86) before/after must match byte-identical. 5) Persist a green->mutated->restored artifact under e2e-evidence/ satisfying MUTATION_SHAPE (verify-mutation-artifact.py:49-54) with fresh evidence, not the stale step-14 log.",
18|  "implementation_sequence": "0) Re-baseline fresh against current dirty tree (not stale cache) -> 1) add 2 new PROBES entries to sdk_probe.py leaving cmd_slash_install/verify untouched -> 2) manual dry-run both new probes -> 3) extend the EXISTING install/verify COMMANDS rows (not new rows) with effects_probe + promoted reached_level -> 4) fix per-arm sandbox isolation -> 5) extend gauge-report.py's full_chain predicate to include the new level -> 6) regenerate docs/v3-gauges.md (never hand-edit) -> 7) verify-counts.py clean -> 8) mutation-proof the new logic -> 9) verify-harness-integrity.py clean -> 10) capture fresh gates-<timestamp> exit-codes.txt -> final: manually re-derive gauge 4 from raw JSON, don't trust prose. Each step has an explicit verification checkpoint before it, detailed in the full report."
19|}