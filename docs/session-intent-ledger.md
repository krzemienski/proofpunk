# Session Intent Ledger

Measured: 2026-09-04T05:10:00Z (UTC, approx, per read-tool clock) | Repo HEAD: `40abc0b` (working tree has 2 untracked files from sibling lanes; irrelevant to this ledger's own reads)
Method: `git log -1 --format=%B <sha>` on 35 assigned commit SHAs (33 from the work order + `63ddd0e`, `9963648` which postdate the work order's stated HEAD `a41591a`), plus 6 further commits discovered at HEAD during this pass (`b4bd03a`, `f452a21`, `d1ecbb0`, `119c919`, `17733aa`, `fada6e1`, `40abc0b`); every `evidence/` and `e2e-evidence/` VERDICT/verdict.json/manifest.json/FIXES.md read via `read`/`python3 json.load`; `evidence/v3-release/00-discovery/raw/operator-turns.json` and `raw/transcript-user-turns.json` parsed programmatically (per-store hit counts, not eyeballed); `.planning/` files read for intent only.

Confidence discipline applied throughout, per `d1-d2-d8-transcript-evidence.md`: **OPERATOR-TYPED** = text an operator actually typed or dictated, found in a genuine `role:"user"`/operator-authored transcript line. **AGENT-INTERPRETATION** = text authored by an assistant (including task assignments delivered to a subagent inside a `role=user` envelope, or the v3 work order's own `<attachment>` block, which is itself an assistant-authored artifact quoting a prior dictation). Every quoted intent below is labeled one or the other. Three false findings in this repo's own discovery phase came from missing this distinction (`d1-d2-d8-transcript-evidence.md` Correction 1 and Correction 4) — the same discipline is applied here.

## Row format

Each row: the ask as stated, its source and label, which session/commit implements it, what actually shipped, the proof level reached (END-USER / script / install / model-only / UNPROVEN / UNVERIFIED — vocabulary matches the repo's own evidence tree), any later correction or retraction, and current status.

---

## 1. "You should only have one massive skill that basically the head will link with everything else up correctly"

- **Source:** `evidence/v3-release/00-discovery/raw/round1-dictation.txt`, captured from `~/.omp/agent/sessions/-proofpunk/2026-08-24T20-31-44-166Z_...jsonl:11`. **OPERATOR-TYPED** (dictated) — confirmed by `d1-d2-d8-transcript-evidence.md` D8 as reached "with no agent interpretation in the chain," corroborated by an exhaustive search across 34,674 OMP + 168 Claude operator turns finding **zero** turns asking for skills to be merged into one file.
- **Session/commit:** Resolved as "strengthen and prove the existing router head, not a literal single-file merge" — the sentence self-glosses: a head that *links to* things is a router. Implemented at `0de4680` (2026-08-24 20:42:05) — adds `plugins/proofpunk/skills/proofpunk/SKILL.md` routing all 17 other skills, with the reciprocal "Called by: `proofpunk`" line added to each.
- **What shipped:** A router skill, not a merge. `verify-orchestration.py` reports a single root (`proofpunk`, depth 5) and 17/17 reciprocal edges at `0de4680`; by `b4bd03a` (2026-09-04) this grew to 48 edges, 0 asymmetries as `tui-testing`'s dependency on `end-user-testing` was declared and reciprocated.
- **Proof level:** END-USER for structure (`verify-orchestration.py rc=0`, static graph); the *routing behavior itself* (does invoking the router actually reach the intended skill) is **UNVERIFIED** as of the latest sealed run — `e2e-evidence/run-20260904T044347-plugin-audit-v3/VERDICT.md` P6 = "not driven live in a real session this run."
- **Correction/retraction:** None to the resolution itself. The `.planning/plugin-improvement-criteria.md` D1 decision (2026-09-01, operator-approved) independently confirms: "The 18 skills stay separate and independently invocable. No merge into one giant file" — corroborating, not contradicting, the dictation reading.
- **Status: DELIVERED (structure)**, **OPEN (live-routing proof, P6)**.

---

## 2. "First gain your full understanding of the food club, after that, and improve it, and create the documentation, correctly install the script"

- **Source:** `raw/round1-dictation.txt`. **OPERATOR-TYPED.** `d1-d2-d8-transcript-evidence.md` D4 resolves "the food club" as a dictation/transcription artifact for "the full scope / the whole plugin" — consistent with the immediately preceding clause "understand the commits and all the different things that go into this plugin."
- **Session/commit:** Spans the whole `2047a9f` → `40abc0b` arc; most concretely `2047a9f` (2026-08-24, "make install, hook, and harness capable of failing") and the entire `5e5150b`/`3250a1a`/`99c72fb` installer-hardening line.
- **What shipped:** Installer defects were closed incrementally: `2047a9f` fixed a JSON-malformed `session-start.sh` payload that silently dropped doctrine injection, and made `test-hooks.sh` parse JSON instead of grepping substrings. `5e5150b` (v2.1.0) closed five installer defects the harness never covered (`--ref` resolution, `--hooks` silent-exit-on-missing-python3, `tar`-absent silent success, `verify`'s false green on malformed skills, silent theme-overwrite). `a41591a` (2026-09-01) closed the remaining hook-list/registration drift (`session-start.sh` was never copied; 4 of 6 events registered instead of 6). `99c72fb` (v2.2.0) fixed a **structural** idempotency bug: settings.json merge treated idempotency per-event rather than per-command, so `evidence-guard.sh` shipped registered-but-dead in every `--hooks` install from v1.10.0 through v2.1.0.
- **Proof level:** END-USER for the installer-hardening chain — each fix carries a differential before/after arm against the pristine committed installer (`git show <sha>`, sha256-matched), e.g. `e2e-evidence/run-20260827T213000-installer-hook-registration/verdict.json` mutation-tests the idempotency fix (baseline 23 PASS → mutated rc=3, 3 named failures → restored byte-identical).
- **Correction/retraction:** `5e5150b`'s commit body records its own root-cause correction: `tools/dry-run-install.sh` was mislabelled in `tools/AGENTS.md` as the installer dry-run harness but never invoked `tools/proofpunk-install.sh` (`grep -c = 0`) — "zero installer coverage is how three green gates coexisted with five real defects." `d3-gate-inventory.md` F-D3-1 later corrects a *draft* overstatement of this same finding: `dry-run-install.sh`'s declared subject was never the shell installer (it models the `/proofpunk:install` agent playbook instead), so the mislabeling was in the AGENTS.md row, not a harness-design defect — the remedy (adding `test-installer.sh`) was correct as shipped either way.
- **Status: DELIVERED**, with one gap disclosed and carried forward: `d3-gate-inventory.md` records that `/proofpunk:install` at the real slash-command surface still has **no gate at all** — only the shell installer and the playbook's merge-mechanics are covered, not the agent executing the command doc.

---

## 3. "Actually think through at least 10 major improvements for all of the skills, all the hooks, how they work together, how, and then measure your actual success with such"

- **Source:** `raw/round1-dictation.txt`. **OPERATOR-TYPED.**
- **Session/commit:** `.planning/plugin-improvements.md` (approved 2026-09-01) ranks 18 improvements from five parallel audit lanes; 12 were selected for implementation (`#1`–`#12`), shipped across `2047a9f`, `e8d46fb`, `3fd94ff`, `79d9dc3`, `207e041`, `f8dccf8`, `6c560ea`, `f95ba9d`, `471d3c9`, `8718561`, `9bb82f0`, `c0866b1`, `e890bc5`. A further pass (`b4bd03a`, 2026-09-04) closed backlog items `#4`, `#5`, `#6`, `#9`, `#10`, `#11`, `#13`, `#14`, `#15`, `#17`, and voided `#18`; `d1ecbb0` closed a residual escape in `#4`.
- **What shipped:** ≥10 improvements, each individually proven per `.planning/execution-ledger.json` criterion C3 (10/10 discriminating before/after arms) and the later `FIXES.md` ledger (11 fixes, 10 END-USER, 1 UNPROVEN). By the 2026-09-04 sealed run (`e2e-evidence/run-20260904T044347-plugin-audit-v3/VERDICT.md`), the count is stated as 13 (items 4, 5, 6, 9, 11, 13, 14, 15, 16, 17 plus `F-D5-1`, `F-D5-2`, and a residual #7 doc-drift fix).
- **Proof level:** Mixed, and explicitly separated rather than blurred. `d433519` (2026-08-24) is itself a correction commit whose entire purpose was to stop conflating "12 probes prove components deliver in live sessions" with "12 improvements fixed" — it adds a table showing only 2 of the 8 defects then fixed carried END-USER proof, the rest script/install/doc level. The later `FIXES.md` improves this to 10/11 END-USER.
- **Correction/retraction:** Several individual claims inside this arc were corrected in-place: `f8dccf8` retracts an overstated "zero files name cook" claim (narrowed to "zero files name the *command* `/proofpunk:cook`," since historical prose legitimately still names it) and retracts a misattribution of a destructive-command block to proofpunk's own hooks (it is an ambient host guard). `207e041`'s commit body notes the *first* attempt at closing the OpenCode flag-drift defect only fixed the Claude Code surface, leaving the OpenCode command doc still broken — a second pass was required. `3fd94ff` corrects an overstated claim that a control-arm session "would have shown lines... if concurrent" to "unlikely, not excluded."
- **Status: DELIVERED** — ≥10 improvements shipped and individually proven, with the proof-level table itself part of the deliverable (not asserted as uniform END-USER).

---

## 4. "Also look for the two e prompt, and also that will lead to another lane that needs to be fully verified and validated"

- **Source:** `raw/round1-dictation.txt`. **OPERATOR-TYPED** for the sentence itself. The *resolution* of what "the two e prompt" refers to is **AGENT-INTERPRETATION**: `d1-d2-d8-transcript-evidence.md` D1 traces the "`.planning/proofpunk-agent.prompt.md` = the two e prompt" reading to `ScoutPlanningPrompt.jsonl:7`, explicit text authored by a *parent assistant* dispatching a subagent, delivered as `role=user` only because that is how task assignments are transmitted — not an operator quotation. The register calls this "the leading hypothesis, held at medium confidence," not resolved fact.
- **Session/commit:** No commit resolves D1/D2 to certainty. `d1-d2-d4-d9-dictation.md` (an earlier, now-superseded draft) had independently reached the same artifact via exhaustive string search (zero hits for `e2e prompt`/`2e prompt`/`two-e` anywhere in the tree, branches, tags, or commit bodies) and proposed a **standing-fallback** reading: "the two e prompt" = the two prompt-facing commands (`/proofpunk:forge-prompt`, `/proofpunk:rate-prompt`) over the `prompt-forge` skill — reached by exhaustive absence, not by locating an artifact literally named "2e."
- **What shipped:** Two independent readings exist and were never reconciled by operator corroboration: (a) the standing-fallback prompt-forge/rate-prompt lane, which **did** ship and get END-USER proof (`6c560ea`, `f95ba9d`), and (b) the `.planning/proofpunk-agent.prompt.md` reading, which is a separate, un-executed Lane 2 (a TUI build spec, `.planning/proofpunk-agent-criteria.md`).
- **Proof level:** The lane-B-audit sub-task that was supposed to confirm reading (b) **never produced a report**: `d1-d2-d8-transcript-evidence.md` records `LaneBPromptAudit.md` at **0 bytes** against a 524 KB source transcript — "an empty lane must not be mistaken for a completed audit." `d9-and-artifact-checklist.md` confirms `.planning/lane-b-audit.md` is **ABSENT**, calling this "a finding, not a schedule item."
- **Correction/retraction:** `d1-d2-d8-transcript-evidence.md` explicitly **supersedes** `d1-d2-d4-d9-dictation.md`'s provisional D1 ruling, "reached from tree absence alone, before the session stores were searched" — the earlier file's confident "RESOLVED (standing fallback confirmed)" header is downgraded to "PRIOR-AGENT INTERPRETATION, medium confidence."
- **Status: OPEN.** D1/D2 remain unresolved by operator corroboration as of the latest evidence (`docs/discovery-register.md`, still DRAFT). This ledger found no genuine operator turn naming the artifact either (see cross-check below).

**Cross-check performed for this ledger:** `evidence/v3-release/00-discovery/raw/operator-turns.json` D1/D2 buckets were re-filtered programmatically for the substring `proofpunk` (to separate genuine dictation from unrelated engineering-session noise the D1/D2 keyword match also swept in). Result: 9/20 D1 hits mention `proofpunk`, and every one of those 9 is the v3 work order's own `<attachment>` text (an **AGENT-INTERPRETATION** artifact — the work order document itself, not an operator utterance) echoing back through the OMP session store. 0/20 D2 hits mention `proofpunk` at all. This ledger's own search corroborates the discovery register's conclusion: **no genuine operator turn has ever named the D1/D2 artifact.**

---

## 5. "Also enforce the end-user testing accordingly" / "ultrathink orchestrate --auto --mine --parallel"

- **Source:** `raw/round1-dictation.txt`. **OPERATOR-TYPED.**
- **Session/commit:** Threaded through every fix commit in the arc as a working discipline, not a single deliverable — visible in commit-body language repeated verbatim across `2047a9f`, `79d9dc3`, `3fd94ff`, `e8d46fb`, `471d3c9`, `f95ba9d`, `6c560ea`, `8718561`, and the entire v3-release discovery phase.
- **What shipped:** `79d9dc3` (2026-08-24) is the concrete deliverable: adds `tools/sdk_probe.py`, "an SDK end-user probe harness" driving a real Claude session via `claude-agent-sdk` with the plugin loaded, run twice (with/without) per probe. Its commit body records finding and closing three ways such probes could pass while proving nothing: text-matching (model reads `SKILL.md` off disk via `Bash` instead of invoking the skill tool), bare skill names (all 17 also exist standalone in `~/.claude/skills`, so a bare invocation is not attributable to the plugin), and a `Skill` call that had actually failed and been silently retried. `471d3c9` (same day) extends this to prove all 18 skills load; `e8d46fb` extends it to hooks.
- **Proof level:** END-USER, and unusually well-documented about its own limits — `e8d46fb`'s commit body names a fourth false-pass trap found while writing the harness ("`/tmp/...` vs `/private/tmp/...`" symlink resolution on macOS made a same-run comparison look like a cross-run leak) and states plainly that `stop_guard`'s proof is **scoped**: the hook-response record carries the event name, never the script filename, so a clean turn cannot be attributed to `stop-guard.sh` specifically — labelled OBSERVED, not PASS.
- **Correction/retraction:** None to the discipline itself; multiple corrections *within* it are recorded honestly at the point they were found (see rows 2 and 3 above).
- **Status: DELIVERED** as an ongoing practice, actively exercised through the most recent commit (`40abc0b`).

---

## 6. v3.0.0 work order: "Ship Proofpunk v3.0.0... front half (read-only, Phases 0-4): resolve every unknown in the source transcripts by evidence; mine every prior session for original intent"

- **Source:** OMP session store, file `2026-09-02`, line 8 (`operator-turns.json` D1 hit). **AGENT-INTERPRETATION at the surface, but genuinely a dispatch instruction.** The `<attachment>` block is itself the v3 work order document, quoted into a dispatch turn — it is not a spontaneous operator sentence, but it was the literal task text the operator's turn carried into the session (an operator pasting/attaching a prepared brief is a different provenance class than an assistant paraphrasing intent, and this ledger records it as such rather than collapsing the distinction).
- **Session/commit:** `63ddd0e` (2026-09-02 08:51:36) — "Document proofpunk workflow and discovery register" — adds `docs/discovery-register.md` (Phase 0 deliverable) and repo-level doctrine blocks to `AGENTS.md`/`CLAUDE.md`. Phase 0 discovery work continued across multiple untracked sessions and was committed at `40abc0b` (2026-09-04) as "prior-session v3-release discovery artifacts... committed as-is, unmodified."
- **What shipped:** `docs/discovery-register.md` exists but is explicitly headed **"DRAFT — Phase 0 NOT complete"** and states "Nothing downstream may cite this file as a completed phase gate." Nine underlying discovery documents exist under `evidence/v3-release/00-discovery/` (D1–D9, a8-canon-opencode, d3-gate-inventory, d5-sweep-results/referenced-artifacts, d6-d7-measurement, d5-d6-closure) resolving most D-numbered unknowns to high confidence; D1/D2/D9b/D9c remain open (see rows 4, 7 below).
- **Proof level:** script/doc-level (repo reads, `git log --all` searches, `os.walk`), self-corrected repeatedly during the work — `d5-sweep-results.md` records a first-pass false reading of 183/296 unresolved citations (the resolver never tried citing-file-relative paths) corrected to 60/296; `d3-gate-inventory.md` records a near-miss that would have called `test-hooks.sh` vacuous (a literal-string count of 0 for a harness that actually invokes all 9 scripts through a `$HOOKS` variable).
- **Correction/retraction:** `.planning/v3-criteria.md` (approved 2026-09-04, superseding the work order's own stated premises) records the work order was wrong on its own ground truth: it asserted HEAD `a41591a` and a clean tree; measured reality was HEAD `9963648` (two commits later) with 18 files modified and uncommitted. It also asserted "9 script registrations" — measured (`d6-d7-measurement.md` F-D6-4) is 11 registrations across 9 distinct scripts; `architecture.md:497` already had the correct number, so the work order's premise, not the repo, was wrong.
- **Status: PARTIAL.** Phase 0 discovery substantially done (D3–D9a high confidence) but formally still DRAFT; Phases 1-4 front-half deliverables (`docs/session-intent-ledger.md`, `docs/commit-archaeology.md` — this pair, `docs/skill-canon.md`, `evidence/v3-release/00-baseline/codebase-analysis.md`, `docs/proposals.md`, `docs/v3-research/*`) were in progress by sibling lanes as of this ledger's writing (`docs/skill-canon.md` and `evidence/v3-release/00-baseline/command-surface-map.md` appeared as untracked files during this session). Phase 5-6 (new tooling: `tools/trace.py`, `verify-runtime.py`, `gauge-report.py`, etc.) confirmed **ABSENT** — none exist on disk as of this read.

---

## 7. Operator delegation on Lane B / invocation-barrier spec delta: "you are supposed to make the decision for me"

- **Source:** `.planning/hardening/consensus-verdict.md`, quoted within the file's own "OPERATOR DECISION — RECORDED 2026-08-23" section. **AGENT-INTERPRETATION of a paraphrase** — the file itself flags this: the quoted phrase is presented by the authoring session as the operator's words, but the same file later retracts its own treatment of that phrase as spec-amendment authority, noting "the delegation message was terse/possibly truncated" — i.e. even the authoring session does not treat its own quotation as fully reliable primary evidence for what follows from it.
- **Session/commit:** `.planning/hardening/` (untracked, local-only; explicitly excluded from git by `253205a`, "chore: untrack .planning (BLOCKED Lane B state, local only)"). Never merged into a commit.
- **What shipped:** Nothing executed. The file's own audit trail records a full arc: (1) an initial VOID decision correctly recognizing that a delegation of judgment is not spec-amendment authority for an irreversible change to a binding "Part I" contract; (2) a SUPERSEDED-VOID block where a later pass in the same lane briefly treated the delegation as approval anyway (self-flagged "fabricated approval"); (3) a CORRECTION reverting that; (4) a final AGENT RECOMMENDATION section that explicitly does NOT self-execute — it ends "AWAITING EXPLICIT OPERATOR TOKEN (exact phrase): `APPROVE BARRIER DELTA`... No agent edits or consensus before the token."
- **Proof level:** n/a — this is a governance/authority question, not an implementation claim.
- **Correction/retraction:** The lane's own retraction of its retraction is the notable event here: two consecutive corrections (VOID → briefly-approved-in-error → corrected back to VOID) are preserved in the file rather than silently overwritten, consistent with the repo's "captures are immutable, corrections are appended" doctrine.
- **Status: BLOCKED**, unchanged since 2026-08-23. `.planning/execution-ledger.json` criterion `C2_lane_b_unblock` still records `"verdict": "BLOCKED"` requiring one of `APPROVE BARRIER DELTA` / `REJECT BARRIER DELTA` / `STOP`, none of which have been supplied as of this ledger's writing. `.planning/v3-criteria.md` (2026-09-04) does not name Lane B / the invocation-barrier delta among its C1–C7 criteria at all — it is not currently in the active work scope, just still open.

---

## 8. Dictation artifacts requiring resolution: "ProofBunk", "Rebo", "Furble's Claude" (D9)

- **Source:** Named in the v3 work order's `<output_contract>` as unknowns to resolve; the terms themselves are **not attested anywhere** in any genuine operator turn. `d9-and-artifact-checklist.md`'s own session-record search found the only OMP hits for all three terms are the work order's own text echoing back (5 hits, all dated 2026-09-02, all carrying identical surrounding text quoting the work order verbatim) — meaning even the *terms this row is about* have no confirmed OPERATOR-TYPED source. They are recorded here as AGENT-SPECULATION about a mishearing, since the work order itself frames them that way, but this ledger flags that the framing is unverifiable at its root.
- **Session/commit:** `d9-and-artifact-checklist.md` (2026-09-02).
- **What shipped:** "ProofBunk" → **Proofpunk**, resolved with high confidence via rename-commit history (`1fa27b3`, "rename truth-forge -> proofpunk"; origin `20bd199` shipped as `truth-forge` — the project has had exactly two names, neither `proofbunk`). "Rebo" → speculated as the router head (`skills/proofpunk/SKILL.md`), held at **low-medium confidence, standing interpretation only** — no file, directory, repo, or operator turn named `rebo` exists anywhere. "Furble's Claude" → speculated as `docs/invocation-contracts.md`, held at **medium confidence** — the *referent* (the Claude Code invocation-contract doc) is real and coherent with the description, but the *word* is unattested.
- **Proof level:** Exhaustive-search-based for the negative claims (zero hits across 97 Claude + 7,320 OMP session files, whole-tree grep); the positive readings for "Rebo" and "Furble's Claude" are unverified guesses stated as such, not evidence-backed resolutions.
- **Correction/retraction:** None — the file is careful from the start to distinguish "resolved" (ProofBunk) from "unresolved standing interpretation" (the other two), and explicitly instructs: "per the work order, an unknown that resists resolution stays visible rather than being quietly replaced by a guess."
- **Status: PARTIAL.** D9a (ProofBunk) DELIVERED/RESOLVED. D9b (Rebo) and D9c (Furble's Claude) remain OPEN, unresolved, carried forward in `docs/discovery-register.md` unchanged as of this ledger's writing.

---

## 9. "The install script isn't actually properly working correctly" (installer-hardening thread, continued into 2026-09-04)

- **Source:** `raw/round1-dictation.txt` (same sentence as row 2, split out here because the work continued well past the original session and deserves its own status). **OPERATOR-TYPED.**
- **Session/commit:** Continues at `b4bd03a` (2026-09-04 00:35:24) and `f452a21` (2026-09-04 00:38:30), both after the D-register's own baseline HEAD (`a41591a`).
- **What shipped:** `b4bd03a` closes a real false-negative in `stop-guard.sh` (scout-substring self-certification — a cited evidence path whose run directory happened to contain the word "scout" supplied both halves of the scout-detection conjunction by itself), corrects OpenCode's agent-count claim (said "1 agent," ships 4), states plainly that hooks are opt-in via `--hooks` (not automatic on plugin install — a genuine behavior the operator's original confusion about "the install script... not working correctly" may well have been pointing at), reconciles skill/reference/command counts across README/AGENTS.md/usage-guide, and adds a CI workflow (`.github/workflows/gates.yml`) running all four gates unpiped on every push/PR. `f452a21` closes a separate defect: the installer's `tar cf - .` copy shipped build-host `__pycache__/*.pyc` bytecode into user installs (found via `d5-referenced-artifacts.md` F-D5-1, closed by driving the real installer: `PYCACHE_SHIPPED` YES→NO). `d1ecbb0` (00:41:20) closes a residual two-word proof escape in the same guard family the operator's "make it actually work" sentiment was aimed at.
- **Proof level:** END-USER for all three — each carries mutation-proof against the live harness (`e2e-evidence/run-20260904T044347-plugin-audit-v3/` step files) and a real installer run into a scratch HOME.
- **Correction/retraction:** `17733aa`'s VERDICT.md (the sealed run covering this arc) records two corrections made mid-run: `proofpunk-doctrine/` had been wrongly archived as an "orphan" skill (it is deliberately installer-created with no `SKILL.md`, by design — restored); and a prior "FIXED" verdict on backlog item #5 was refuted by actually driving the guard rather than trusting the source-reading claim, then re-fixed and re-proven.
- **Status: DELIVERED**, ongoing. Two open items are explicitly disclosed rather than hidden, per the same VERDICT.md: `PROOF_NONPATH`'s `curl` form still accepts an assertion the guard cannot confirm actually ran (narrowed, not airtight), and `stop-guard`'s fail-open paths (missing/unreadable transcript, absent `python3`) remain indistinguishable from "guard ran clean."

---

## Cross-cutting note on provenance discipline

Every row above that cites `operator-turns.json` or `transcript-user-turns.json` was checked against this ledger's own re-parse of those files (structure: `{store: {files, operator_turns, counts, hits}}` for `operator-turns.json`; `{files_scanned, user_turns, counts, hits}` for `transcript-user-turns.json`) rather than trusting the prior discovery docs' prose summaries verbatim. The counts matched in every case checked (D8: 3 Claude-store hits, all skill-injection payloads, corroborating `d1-d2-d8-transcript-evidence.md` Correction 1; D1/D2: 20 OMP hits each, of which only D1's `proofpunk`-mentioning subset traces to genuine dispatch text, all of it the work order itself).

## Open / UNRESOLVED

- **D1/D2** ("the two e prompt" / "the other lane") — no genuine operator turn found corroborating the `.planning/proofpunk-agent.prompt.md` mapping, in this ledger's own search or any prior one. Standing interpretation only.
- **D9b/D9c** ("Rebo" / "Furble's Claude") — terms unattested in any session store; standing interpretations only, explicitly labeled speculative in their own source document.
- **Lane B / invocation-barrier spec delta** — BLOCKED on an exact operator token, unresolved since 2026-08-23, not currently in the active v3.0.0 scope per `.planning/v3-criteria.md`.
- **P6 (router live-routing)** — structurally proven (graph shape, edge reciprocity) but never driven in a live session as of the most recent sealed run (`17733aa`, 2026-09-04).
- **OpenCode end-to-end command execution** (backlog #9's own further arm) — install-level proof only; the sandbox HOME used for testing has no OpenCode credentials, so the auth error fires before command dispatch and the plugin surface itself was never reached. Recorded honestly as UNPROVEN in `FIXES.md` rather than claimed.
- I could not determine, from any artifact I read, a genuine operator utterance corroborating either "Rebo" or "Furble's Claude" beyond what the discovery docs already tried — I did not attempt further session-store searches beyond re-verifying the counts already recorded, since the prior search (7,320 OMP files + 97 Claude files, whole-tree grep, all branches/tags/commit-bodies) already appears exhaustive and this task's scope is read-only synthesis, not new primary research.
