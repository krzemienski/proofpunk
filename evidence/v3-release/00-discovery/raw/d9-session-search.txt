## store=claude files_scanned=97
  proofbunk: 0 operator hits
  rebo: 0 operator hits
  furble: 0 operator hits
## store=omp files_scanned=7320
  proofbunk: 5 operator hits
      2026-09-02T08-31:8 …by `build-site.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- n
      2026-09-02T08-18:8 …by `build-site.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- n
      2026-09-02T07-19:7 …by `build-site.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- n
      __advisor.jsonl:3 …by `build-site.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no
      __advisor.jsonl:3 …by `build-site.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no
  rebo: 5 operator hits
      2026-09-02T08-31:8 …ite.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator 
      2026-09-02T08-18:8 …ite.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator 
      2026-09-02T07-19:7 …ite.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator 
      __advisor.jsonl:3 …ite.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator r
      __advisor.jsonl:3 …ite.py`). Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator r
  furble: 5 operator hits
      2026-09-02T08-31:8 …Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator round-trip
      2026-09-02T08-18:8 …Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator round-trip
      2026-09-02T07-19:7 …Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator round-trip
      __advisor.jsonl:3 …Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator round-trip.
      __advisor.jsonl:3 …Basis: `AGENTS.md` No runtime dependencies. Impact if wrong: high -- every new tool below is specified stdlib-only.  - ProofBunk, Rebo, and Furble's Claude are treated as dictation artifacts for D9 to resolve, with standing interpretations recorded there -- no operator round-trip.
