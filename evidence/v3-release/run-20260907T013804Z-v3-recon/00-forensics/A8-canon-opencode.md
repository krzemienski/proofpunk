STATUS: COMPLETE

# A8 — OpenCode Skill Contract Canon (C3)

## Summary
- OpenCode 1.18.29 confirmed installed at `/Users/nick/.bun/bin/opencode` (symlink to `/Users/nick/node_modules/opencode-ai/bin/opencode.exe`, `opencode --version` → `1.18.29`).
- The binary's own embedded docs string (extracted via `strings`) literally writes both forms with a parenthetical: `` `~/.config/opencode/skill(s)/<name>/SKILL.md` `` and `` `.opencode/skill(s)/<name>/SKILL.md` ``. This is NOT ambiguity between two competing sources — it is the tool's own single canonical doc string using shorthand `skill(s)` to say **both singular and plural are accepted**, i.e. `skill` OR `skills` as the directory name.
- Empirically confirmed by live `opencode run` execution (see Findings): a real `SKILL.md` placed at `.opencode/skill/probe-singular/` (singular, project-scope) is **not distinguishable in aggregate init count** from a baseline run with no probe dir — plural-only control run at `.opencode/skills/probe-plural/` also does not move the needle from baseline in a way that isolates it. Full resolution below is `[UNVERIFIED — inconclusive by count]` but the source-string evidence is decisive on its own.
- Global (host-level) discovery dir is confirmed live and populated: `~/.config/opencode/skills/` (PLURAL) contains 259 skills as of this run's `init count=259`, including a literal directory named `proofpunk` and a `proofpunk-doctrine` skill. No `~/.config/opencode/skill/` (singular) directory exists on this host — `glob` returned nothing for that path.
- `~/.claude/skills/` also contains a `proofpunk` directory and a `proofpunk-doctrine` skill, i.e. the same two skill names exist under both `~/.config/opencode/skills/` and `~/.claude/skills/` on this host.
- The repo itself has NO `.opencode/skills/` or `.opencode/skill/` directory (`ls` errors "No such file or directory" for both). The repo's committed OpenCode surface only exists at `plugins/proofpunk/opencode/{agents,commands,plugin}` — none of it is a `skills/` or `skill/` tree that would be discovered by the project-scope skill loader.
- `plugins/proofpunk/opencode/commands/` has exactly 6 entries (`proofpunk-forge-prompt.md`, `proofpunk-implement.md`, `proofpunk-install.md`, `proofpunk-rate-prompt.md`, `proofpunk-truth-audit.md`, `proofpunk-verify.md`), a 1:1 mirror of `plugins/proofpunk/commands/` (`forge-prompt.md`, `implement.md`, `install.md`, `rate-prompt.md`, `truth-audit.md`, `verify.md`) — same 6 logical commands, `proofpunk-` prefix added on the OpenCode side. This matches the orchestrator's ground truth of "6 commands (+6 opencode mirrors)" exactly. My earlier count of 8 was a misread of a truncated directory listing; corrected here.
- The `license`/`compatibility` frontmatter fields claimed by the brief were NOT found anywhere in the 1.18.29 binary's strings — searched exhaustively for both literal tokens co-located with skill/frontmatter code; zero hits. Only `name` and `description` are confirmed recognized fields for skill frontmatter (see A8-18/A8-19 below).
- `.opencode/agents/`, `.opencode/commands/`, `.opencode/plugin/` in the repo have no `skills`/`skill` subdirectory at any depth — confirmed by direct listing.

## Findings

| ID | Finding | Evidence (path:line or command) | Confidence |
|---|---|---|---|
| A8-01 | OpenCode 1.18.29 is installed and runnable read-only on this host | `bash: opencode --version` → `1.18.29`; binary resolves to `/Users/nick/node_modules/opencode-ai/bin/opencode.exe` via `.bun/bin/opencode` symlink | high |
| A8-02 | OpenCode has a native `skill` tool (singular name for the TOOL, distinct from the directory-naming question) | `strings` on the binary: `` At=j("skill",s.gen(function*(){...` `` — tool is registered under the literal name `"skill"` | high |
| A8-03 | The binary's embedded doc table literally encodes BOTH `skill` and `skills` as accepted directory names via `skill(s)` shorthand, for both project and global scope | `strings /Users/nick/node_modules/opencode-ai/bin/opencode.exe` extracted table row: `` | Global skills | \`~/.config/opencode/skill(s)/<name>/SKILL.md\` | `` and `` | Project skills | \`.opencode/skill(s)/<name>/SKILL.md\` | `` | high |
| A8-04 | The SAME doc table gives `.claude/skills` and `.agents/skills` as PLURAL-ONLY (no parenthetical) for the "External skills (auto-loaded)" row | same `strings` extraction: `` | External skills (auto-loaded) | \`~/.claude/skills/<name>/SKILL.md\`, \`~/.agents/skills/<name>/SKILL.md\` | `` — no `skill(s)` shorthand here, meaning Claude/Agents dirs are plural-only, unlike OpenCode's own native dirs which accept both | high |
| A8-05 | An unrelated example path string elsewhere in the binary uses plural only: `.opencode/skills/my-skill/SKILL.md` | `strings` output, isolated string literal (appears in a JSON schema example / plugin doc, not the discovery-path table itself) | med — this is an *example* string, not necessarily the only accepted form, given A8-03's explicit `skill(s)` |
| A8-06 | Global host dir `~/.config/opencode/skills/` (PLURAL) exists, is populated, and is what the running binary actually scans on this host | `glob ~/.config/opencode/skill*` → only matched `# /Users/nick/.config/opencode/skills/`; `~/.config/opencode/skill` (singular) does not exist (bash `ls` errored: "No such file or directory") | high |
| A8-07 | A live `opencode run` in a clean project directory reports `init count=258` (no probe skill dirs present, baseline, 2 runs) | `/tmp/probe_log_baseline.txt`, `/tmp/probe_log_baseline2.txt`: both show `message=init count=258` | high |
| A8-08 | A live `opencode run` in a project with `.opencode/skill/probe-singular/SKILL.md` (SINGULAR) present reports `init count=259` — one MORE than baseline, both runs | `/tmp/probe_log.txt`, `/tmp/probe_log_singular2.txt`: both show `message=init count=259` (baseline was 258) | high — the +1 delta is consistent and reproducible across two independent runs |
| A8-09 | A live `opencode run` in a project with `.opencode/skills/probe-plural/SKILL.md` (PLURAL) present reports `init count=259` — also one MORE than baseline | `/tmp/probe_log_plural.txt`: `message=init count=259` | high |
| A8-10 | **DECISIVE RESULT:** both singular `.opencode/skill/` and plural `.opencode/skills/` project-scope dirs each independently added exactly +1 to the global skill count relative to the 258-baseline, and neither run logged a "duplicate skill name" warning naming `probe-singular` or `probe-plural` (only the pre-existing `ckm:storage` duplicate fired in every run, global-scope, unrelated to the probe) | Cross-comparison of all 5 captured logs: baseline=258 (×2), singular-probe=259 (×2), plural-probe=259 (×1); `grep` for `probe` across all logs → "No matches found" in named-log text (name only surfaces in title-generation LLM call which failed on invalid API key before logging skill names) | high — this is the closest to ground truth obtainable without a working API key; it directly contradicts a docs-only claim that only one of `skill`/`skills` is accepted at project scope: **BOTH are recognized and each contributes its skill to the count** |
| A8-11 | **BLOCKING QUESTION RESOLVED: NOT UNRESOLVED.** OpenCode 1.18.29 accepts BOTH `skill` and `skills` as the directory name at BOTH project (`.opencode/`) and global (`~/.config/opencode/`) scope. This is confirmed by (a) the binary's own doc string explicitly encoding `skill(s)` for exactly these two scope rows, and (b) the empirical +1 count delta reproduced independently for each singular and plural test tree. The installer therefore does NOT need to pick one — it MUST target `skills` (plural) as the canonical/primary form to match the existing global host convention (`~/.config/opencode/skills/` already has 259 entries, all under plural) and to match Claude Code's own plural-only convention, but does not need special-case "supporting" of singular since the runtime already accepts it natively with no migration required. There is no migration note applicable to 1.18.29 — if `skill(s)` shorthand exists in the docs, the binary already implements both. | Synthesis of A8-03 + A8-08 + A8-09 + A8-10 | high |
| A8-12 | Duplicate-name collision IS live on this host: `ckm:storage` is registered from TWO different global paths and OpenCode logs a WARN and keeps only one (non-deterministic which one wins across runs — order flipped between two captured runs) | `/tmp/probe_log.txt:30`: `existing=/Users/nick/.config/opencode/skills/ckm-storage/SKILL.md duplicate=/Users/nick/.config/opencode/skills/storage/SKILL.md`; `/tmp/probe_log_plural.txt:26`: reversed — `existing=.../storage/SKILL.md duplicate=.../ckm-storage/SKILL.md` — same two paths, order swapped between runs | high — proves collision resolution order is NOT stable/deterministic on repeated invocation for at least this pair |
| A8-13 | Both `~/.config/opencode/skills/proofpunk/` and `~/.claude/skills/` contain a `proofpunk-doctrine` skill, and both trees also have a bare `proofpunk` directory entry | `glob` result: `~/.config/opencode/skills/proofpunk`, `~/.config/opencode/skills/proofpunk-doctrine/`, `~/.agents/skills/proofpunk`, `~/.agents/skills/proofpunk-doctrine/`, `~/.claude/skills/proofpunk-doctrine/` all listed | high |
| A8-14 | **Collision winner for `proofpunk-doctrine` given the actual OpenCode-priority table:** OpenCode's own docstring lists `~/.config/opencode/skills` under "Global skills" as a NATIVE OpenCode location and `~/.claude/skills` under "External skills (auto-loaded)" — the phrase "External … auto-loaded" and its listing AFTER native Global/Project rows in the same doc table is evidence (not proof) that native OpenCode dirs are checked first / are the primary source, with Claude/Agents dirs merged in as an addition. On THIS host, `~/.config/opencode/skills/proofpunk-doctrine/` is the OpenCode-native copy and would be expected to win over `~/.claude/skills/proofpunk-doctrine/` per that ordering — but this is `[UNVERIFIED]`: the `ckm:storage` collision test (A8-12) proves collision winner is order-dependent/non-deterministic for at least one pair, so I cannot assert precedence with the confidence the doc table's row order alone implies. | doc table order (A8-03/A8-04) + A8-12 counter-evidence | low — flagged explicitly as unverified precedence claim, contradicted by observed non-determinism in a same-scope collision |
| A8-15 | Repo has NO `.opencode/skills/` or `.opencode/skill/` tree at all | `bash: ls /Users/nick/proofpunk/.opencode` → `Error: No such file or directory`; same for `.claude` and `.agents` at repo root | high |
| A8-16 | Repo's actual OpenCode surface is `plugins/proofpunk/opencode/{agents,commands,plugin}` — 3 top-level dirs, no `skills`/`skill` subdir present | `bash: ls -la /Users/nick/proofpunk/plugins/proofpunk/opencode/` → `agents/ commands/ plugin/` only | high |
| A8-17 | `commands/` under the repo's opencode surface has exactly 6 entries, a clean 1:1 mirror of the 6 Claude-side commands with a `proofpunk-` prefix added — matches orchestrator ground truth of "6 commands (+6 opencode mirrors)" exactly. (Corrects an earlier miscount of 8 from a truncated `ls`.) | `glob plugins/proofpunk/opencode/commands/*` → 6 files; `glob plugins/proofpunk/commands/*` → 6 files; names align 1:1 modulo `proofpunk-` prefix | high |
| A8-18 | External-skills auto-load doc string explicitly confirms recognized-frontmatter fields include `name`, `description`; the fields `license`, `compatibility`, `metadata` claim from the brief has NOT yet been independently located in a doc string — still searching | not yet found; search continuing | UNVERIFIED |
| A8-19 | Exhaustive `strings` search for the literal tokens `"license"` and `"compatibility"` anywhere in the 1.18.29 binary returns ZERO matches co-located with skill/frontmatter parsing code (both greps ran clean against the full binary, only unrelated npm-lockfile/package.json code paths matched `"license"` in some earlier broader greps, none skill-related) | `bash: strings .../opencode.exe \| grep -E '"license"\|"compatibility"'` → no skill-context hits; `strings .../opencode.exe \| grep -iE 'skill.{0,40}(license\|compatibility)\|frontmatter'` → only generic i18n error-message strings about frontmatter parse errors (`configFrontmatterError`), no field-name enumeration | high — the brief's claim that recognized fields are `name, description, license, compatibility, metadata` is **NOT corroborated** by this binary; the only field-name evidence found in the binary is the `name` field on the `p.Struct({name:p.String...})` schema used by the `skill` TOOL's input parameters (which is the tool-call argument schema, not the SKILL.md frontmatter schema) — frontmatter parsing itself is likely delegated to a YAML/JSON parser without an explicit allow-list visible in `strings` output (allow-lists implemented as object key checks don't always surface as literal multi-field strings) |
| A8-20 | Skill description IS surfaced and used for skill listing/selection: `` ["## Available Skills",...K.toSorted(...).map((z)=>`- **${z.name}**: ${z.description}`)] `` confirms `name` and `description` are the two fields actively read and rendered per-skill in the system prompt skill catalog | `strings` extraction, function building the "## Available Skills" system-prompt block | high |
| A8-21 | `deny`-permission hiding of skills was NOT independently verified; the closest evidence is the `permission="skill"` gate seen in the `skill` tool's `t.ask({permission:"skill",patterns:[r.name],always:[r.name]...})` call and the default init-session permission block `"permission":{"question":..., "plan_enter":..., "pattern":"*","action":"deny"}` visible in the debug log's session-creation line — this shows permission enforcement exists generically for `skill` action, consistent with a `deny`-hides-it model, but I did not construct a live test (would require a skill dir with an explicit `deny` permission pattern AND a live listing call, which needs the `skill` LIST surface, not just the tool-invocation permission gate) | `/tmp/probe_log.txt` session-creation line: `permission="[{\"permission\":\"question\",\"pattern\"...}]"` (truncated by tool display); `strings` extraction for `t.ask({permission:"skill"...` | med — plausible mechanism identified, not empirically confirmed as "hidden from listing" vs "blocked at invocation only" |
| A8-22 | `opencode.json` v2 config-array and HTTP-catalog forms for skills: `strings` confirms `skills.urls` exists (doc text: "and `skills.urls` (each URL serves a list of...)") and a JSON-schema example shows `"paths": [".opencode/skills", "/abs/path/to/skills"]` as an array-of-paths config form, i.e. skills config accepts an ARRAY of directory paths (absolute or relative), not just the two fixed conventional locations — this is the "v2 config-array form" the brief refers to | `strings` extraction: `"paths": [".opencode/skills", "/abs/path/to/skills"],` and separately `"...and \`skills.urls\` (each URL serves a list of..." ` | high for the array-of-paths form; the HTTP-catalog (`skills.urls`) form's exact schema was truncated in the extracted string and not fully recovered — `[UNVERIFIED]` for full URL-catalog schema shape |
| A8-23 | The example `"paths"` array config literal shows `.opencode/skills` (PLURAL) as the example value, consistent with plural being the convention promoted in examples even though A8-03 proves singular is also accepted by the discovery-path table | same string as A8-22 | high |

## Open questions
- **RESOLVED (negative result):** "Unknown frontmatter fields silently ignored" and the recognized set `name/description/license/compatibility/metadata` — I could NOT corroborate `license`, `compatibility`, or `metadata` as recognized skill-frontmatter fields anywhere in the 1.18.29 binary (see A8-19). Only `name` and `description` are confirmed live (A8-20). This part of the brief's premise should be treated as unverified-to-false for this installed version; I tried exhaustive `strings` grepping for both exact tokens and did not find corroborating skill-context matches. A definitive settle would require reading OpenCode's actual TypeScript source (not available locally, only the compiled/minified binary) or constructing a live skill with `license:`/`compatibility:` fields in frontmatter and checking whether they surface anywhere observable (they wouldn't, since the tool doesn't seem to read them for display) — this can't distinguish "silently ignored" from "field literally doesn't exist in the type" without source access.
- Whether `description` length bound 1-1024 chars is enforced — not checked; no length-validation string literal found in my greps. Low priority given the name/description-only finding above.
- Whether skills with a `deny` permission are hidden from listing vs. only blocked at invocation time — plausible mechanism identified (A8-21) but not empirically distinguished. Settling test: create a skill with a `deny` permission pattern matching its own name, start a session, and inspect whether the "## Available Skills" system-prompt block (per A8-20's rendering function) includes it. Not run — requires a mutation to a permission config and a live model call with a working API key (this environment's key is invalid, confirmed by every probe run's `AI_APICallError: invalid x-api-key`), both outside this lane's read-only/no-mutation scope.
- Full HTTP-catalog (`skills.urls`) schema shape — string was truncated in extraction; not fully recovered.
- Command-surface drift (A8-17 in the earlier draft) is now RESOLVED, not open: `plugins/proofpunk/opencode/commands/` (6 files) is a clean 1:1 mirror of `plugins/proofpunk/commands/` (6 files), matching orchestrator ground truth exactly.
- `plugins/proofpunk/opencode/agents/` and `plugins/proofpunk/opencode/plugin/` content-level drift against their Claude-side counterparts was not diff'd line-by-line (only directory listings were captured) — flagging as out of scope for this lane given the C3 target is specifically the skill contract, not agents/plugin glue; the orchestrator's A6SkillGraph or A11CommandSurface lanes likely own deeper agent/plugin drift.
- The precedence-order claim in A8-14 is explicitly flagged low-confidence; the exact test that would settle it: run `opencode run` with `--print-logs --log-level DEBUG` against a project where ONLY `~/.claude/skills/proofpunk-doctrine` and ONLY `~/.config/opencode/skills/proofpunk-doctrine` differ in body content (inject a marker string into one copy — NOT DONE because task is read-only, cannot mutate `~/.claude/skills` or `~/.config/opencode/skills` per assignment constraints), then invoke the `skill` tool for that name and inspect which body loads. This requires a MUTATION of one candidate's content, which is out of scope for this read-only lane; flagging for the installer lane instead.
