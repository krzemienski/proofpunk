# A2 — State Counts (D6 canonical counts, D7 installed-vs-repo)

STATUS: COMPLETE

## Summary
- Repo skills: 18 dirs under `plugins/proofpunk/skills/` — matches `AGENTS.md:27,29,30` claim ("18 skill dirs") exactly. No discrepancy.
- Repo commands: 6 claude (`commands/`), 6 opencode (`opencode/commands/`) — matches `AGENTS.md:28` ("6 slash commands"). `omp/` and opencode have 3+4 agents (see below), not commands. 1 extension file per platform (`extensions/proofpunk.ts` for OMP, `opencode/plugin/proofpunk.ts` for OpenCode — two files, functionally parallel implementations of the same doctrine guard, NOT copies of each other).
- Hooks: 9 `.sh` scripts, 7 top-level event keys, 11 individual command registrations — all three numbers match `AGENTS.md:29` verbatim ("9 hook scripts ... 7 event keys ... 11 registrations"). No discrepancy.
- References: 14 files under `plugins/proofpunk/references/`.
- Agents: 3 Claude/OMP agents (end-user-validate, implement, scout); OpenCode has a 4th (`proofpunk.md`, the router agent) — this asymmetry is DOCUMENTED and intentional per `AGENTS.md:30`: "OpenCode adds a 4th (`opencode/agents/proofpunk.md`, the router)."
- Repo-root `tools/` (NOT `plugins/proofpunk/tools/` — that path does not exist): 21 entries; `fresh_evidence.py` is NOT among them — the real path is `plugins/proofpunk/skills/end-user-testing/scripts/fresh_evidence.py` (per Main's F-002 context, confirmed by glob).
- Repo `package.json` version: `2.2.0` (`plugins/proofpunk/package.json:3`) — NOT bumped despite HEAD being 10 commits past the brief's stale `a41591a` snapshot and despite the current HEAD commit message calling this "v3". The version manifest is stale relative to the orchestrator's "v3" framing.
- Installed marketplace plugin: `~/.claude/plugins/cache/proofpunk-marketplace/proofpunk/2.2.0/` — pinned to 2.2.0, consistent with repo manifest.
- **CRITICAL D7 finding: `~/.claude/skills/` has ZERO standalone copies of any of the 18 canonical proofpunk skill names.** Individually verified absent: `implement`, `brainstorm`, `visual-inspection`, `root-cause-debugging`, `session-intent`, `production-readiness`, `stack-testing`, `tui-testing`, `codebase-truth-audit`, `mobile-validation-runner`, `plan-hardening`, `prompt-forge`, `red-team-eval`, `full-functional-audit`, `validation-plan`, `ui-experience-audit`, `end-user-testing`, `proofpunk` — all returned "not found". A directory named `proofpunk-doctrine/` exists but is an unrelated, differently-named legacy bundle (see below); it does not collide with the plugin skill namespace.
- **CRITICAL D7 finding, opposite result for OMP: `~/.omp/agent/skills/` has ALL 18 canonical skill names installed as LIVE standalone directories, most recently modified 3 days ago** — `visual-inspection`, `validation-plan`, `ui-experience-audit`, `tui-testing`, `stack-testing`, `session-intent`, `root-cause-debugging`, `red-team-eval`, `proofpunk`, `prompt-forge`, `production-readiness`, `plan-hardening`, `mobile-validation-runner`, `implement`, `full-functional-audit`, `end-user-testing`, `codebase-truth-audit`, `brainstorm`. Plus dozens of timestamped `.name.bak-YYYYMMDD-HHMMSS/` backup copies of each, going back to at least 2026-08-11, proving this standalone-copy mechanism has existed and been repeatedly re-synced for nearly a month.
- Orchestrator's `implement` diff (repo sha256 `5ed7370e…` vs global sha256 `da445828…`) is CONFIRMED and characterized below: the only difference is two citation paths (`../../references/X.md` in repo vs `references/X.md` in the OMP standalone copy) — i.e. the OMP copy already has the installer's citation-rewrite applied, while the repo source (by design) keeps the uninstalled `../../` form.

## D6 — Canonical Count Table

| Item | Measured value | Command | Doc claims different number? |
|---|---|---|---|
| Skills (dirs) | 18 | `glob plugins/proofpunk/skills/*` | Matches `AGENTS.md:8,27` ("18 skills"), `README.md:83` ("18 skills") |
| Commands (claude) | 6 | `glob plugins/proofpunk/commands/*` (rate-prompt.md, truth-audit.md, verify.md, forge-prompt.md, implement.md, install.md) | Matches `AGENTS.md:8,28` ("6 commands") |
| Commands (opencode) | 6 | `glob plugins/proofpunk/opencode/commands/*` (proofpunk-rate-prompt.md, proofpunk-truth-audit.md, proofpunk-verify.md, proofpunk-forge-prompt.md, proofpunk-implement.md, proofpunk-install.md) | Matches claude count; no separate doc claim found |
| Commands (omp) | N/A — omp has agents not commands | `glob plugins/proofpunk/omp/**` → only `omp/agents/{end-user-validate,implement,scout}.md` | see agents row |
| Commands (extensions) | 2 files (`extensions/proofpunk.ts` + `opencode/plugin/proofpunk.ts`) — NOT copies of each other, see diff below | `glob plugins/proofpunk/extensions/*` + `glob plugins/proofpunk/opencode/plugin/*` | No doc count found for these |
| Hook `.sh` files | 9 | `glob plugins/proofpunk/hooks/*.sh` (bash-write-notice, bash-write-snapshot, stop-guard, capture-guard, evidence-guard, no-test-files, post-write-walkthrough, session-start, instructions-loaded) | Matches `AGENTS.md:8,29` ("9 enforcement hook scripts") |
| Hook event keys (top-level in hooks.json) | 7 | manual count of `plugins/proofpunk/hooks/hooks.json` top keys under `.hooks`: SessionStart, Stop, SubagentStop, PreToolUse, InstructionsLoaded, PostToolUse, PostToolUseFailure | Matches `AGENTS.md:29` ("7 event keys") |
| Hook registrations (individual command entries) | 11 | manual count of leaf `{"type":"command","command":...}` objects in hooks.json: session-start.sh(1) + stop-guard.sh(Stop,1) + stop-guard.sh(SubagentStop,1) + [no-test-files.sh, evidence-guard.sh, capture-guard.sh](PreToolUse Write\|Edit,3) + bash-write-snapshot.sh(PreToolUse Bash,1) + instructions-loaded.sh(1) + post-write-walkthrough.sh(PostToolUse Write\|Edit,1) + bash-write-notice.sh(PostToolUse Bash,1) + bash-write-notice.sh(PostToolUseFailure Bash,1) = 11 | Matches `AGENTS.md:29` ("11 registrations") |
| References | 14 | `glob plugins/proofpunk/references/*` | Matches `docs/architecture.md:31,526` explicitly ("14 references... the 14th is `run-trace-schema.md`") |
| Agents | 3 (Claude/OMP) + 1 extra (OpenCode router) = 4 unique agent files, 3 shared | `glob plugins/proofpunk/agents/*` (end-user-validate.md, implement.md, scout.md) — mirrored identically under `omp/agents/`; `opencode/agents/` has these 3 plus `proofpunk.md` | Matches `AGENTS.md:30` exactly ("3 Claude Code subagents... OpenCode adds a 4th... OMP mirrors the Claude Code 3") |
| Themes — hyper (js) | 20 | `glob plugins/proofpunk/themes/hyper/*.js` | Matches `AGENTS.md:8` ("20-theme pack") |
| Themes — omp (json) | 20 | `glob plugins/proofpunk/themes/omp/*.json` | Matches |
| Themes — opencode (json) | 20 | `glob plugins/proofpunk/themes/opencode/*.json` | Matches |
| Themes — total unique palettes | 20 named palettes (night-drive, overdrive, psy-wave, static-noir, void-orchid, bio-lumen, chrome-rose, cold-fusion, solar-flare, amber-phosphor, code-fall, laser-lime, miami-vice, ultraviolet, ghost-shell, ion-storm, red-line, vapor-grid, acid-rain, neon-tokyo) × 3 formats = 60 files | manual cross-check of 3 glob results | Consistent — AGENTS.md says "20-theme pack" (palette count, not file count) |
| `tools/` (repo root) entries | 21 | `glob tools/*` | N/A (brief says `tools/fresh_evidence.py` doesn't exist — CONFIRMED per Main's F-002 context, real path is `plugins/proofpunk/skills/end-user-testing/scripts/fresh_evidence.py`) |
| Repo `plugins/proofpunk/package.json` version | 2.2.0 | `read plugins/proofpunk/package.json` | Brief claims HEAD=a41591a/v2.2.0 (stale per orchestrator); manifest still literally says 2.2.0 even at current HEAD 93c479d — so version string has NOT been bumped despite orchestrator calling this "v3" |

### Resolved: opencode's 4th agent
`glob plugins/proofpunk/opencode/agents/*` returned 4 files: end-user-validate.md, implement.md, **proofpunk.md**, scout.md — one more than the 3-agent set shared by `plugins/proofpunk/agents/` and `plugins/proofpunk/omp/agents/`. `proofpunk.md` (read in full) is a `mode: primary` doctrine-router persona for OpenCode: it states the same 5-point doctrine (execution-first, end-user-testing-only-proof, no mocks, distill-criteria-first, criteria-proof-table) and tells the agent to activate other proofpunk skills as needed. `AGENTS.md:30` confirms this is intentional: "OpenCode adds a 4th (`opencode/agents/proofpunk.md`, the router)." Not a defect.

### Resolved: the two `.ts` extension files are NOT copies
`plugins/proofpunk/extensions/proofpunk.ts` (OMP) and `plugins/proofpunk/opencode/plugin/proofpunk.ts` (OpenCode) implement the same doctrine-guard behavior (destructive-command blocking regexes, secret-path pattern, session-start notification) against two DIFFERENT plugin APIs: OMP's `ExtensionAPI` (`pi.on("session_start", ...)`, `pi.on("tool_call", ...)`, returns `{block, reason}`) vs OpenCode's `Plugin` type (`"tool.execute.before"` hook, throws `Error` to block). 165-line diff confirms: same regex patterns, same secret-path definition, structurally parallel but API-specific rewrites — not accidental drift, not a stale copy of one another.

## D7 — Installed vs Repo State

### `~/.claude/skills/`
**CORRECTION to an earlier draft of this section**: an initial pass using an image-rendered directory listing (hard to read precisely at scale) misattributed a batch of `.name.bak-YYYYMMDD-HHMMSS/` backup entries as live top-level collisions in `~/.claude/skills/`. Those backup entries actually live under `~/.omp/agent/skills/` (a completely different directory tree — see next section), not here. This section replaces that draft with results individually verified via direct `read` on each canonical name.

- Individually verified via direct path lookup for all 18 canonical skill names as top-level entries of `~/.claude/skills/`: **ALL 18 returned "not found"** — `implement`, `brainstorm`, `visual-inspection`, `root-cause-debugging`, `session-intent`, `production-readiness`, `stack-testing`, `tui-testing`, `codebase-truth-audit`, `mobile-validation-runner`, `plan-hardening`, `prompt-forge`, `red-team-eval`, `full-functional-audit`, `validation-plan`, `ui-experience-audit`, `end-user-testing`, `proofpunk`.
- **`~/.claude/skills/` has ZERO standalone copies of the 18 canonical proofpunk skill names.**
- A directory named `proofpunk-doctrine/` DOES exist at the top level of `~/.claude/skills/` — but it is NOT one of the 18 canonical names, and it is a different, unrelated, older bundle (contains `THIRD_PARTY_NOTICES.md`, `goal-loop-orchestrator.skill`, `prompt-engineer.skill`, `northstar.skill`, `.scout.skillkit.json`, `agent_skills_spec.md`, `analyze.zip`, `humanize-blog-post.zip`, `visual-inspection.zip`, `yt-shorts-debugging.md`) plus 14 loose `.md` files sharing basenames with the plugin's `references/*.md` (api-validation.md, ci-gates.md, cli-validation.md, defect-pattern-database.md, end-user-actor.md, evidence-contract.md, ios-hig-checklist.md, ios-validation.md, platform-routing.md, preflight-checks.md, severity-model.md, web-validation.md, web-wcag-checklist.md, README.md). This is a basename collision on loose reference files, NOT a skill-directory collision — and its directory name (`proofpunk-doctrine`, not `proofpunk`) means a bare skill lookup for any of the 18 canonical names never resolves here.
- A sibling `truth-forge-doctrine/` directory exists with the identical 14-file pattern — evidence proofpunk was previously named "truth-forge" and this is a stale predecessor-named copy, also not colliding with any of the 18 current canonical names.
- Two explicitly-named `_retired-proofpunk-stale-20260902-030412/` and `_retired-proofpunk-stale-20260902-030220/` directories exist at top level, containing an OLDER flat-file (not directory-per-skill) install layout (`truth-forge-verify.md`, `truth-forge-rate-prompt.md`, `truth-forge-implement.md`, `proofpunk-verify.md`, `proofpunk-forge-prompt.md`, `proofpunk-implement.md`, `proofpunk-install.md`, `proofpunk-rate-prompt.md`, `mermaid-iframe-author.md`) — proof of at least two prior install generations, both explicitly marked retired and thus out of the live resolution path.

**ANSWER for `~/.claude/skills/`: a bare (non-namespaced) invocation of any of the 18 canonical proofpunk skill names resolves to the PLUGIN, not to a standalone copy — because no standalone copy of any of those 18 names exists here.** Evidence: 18/18 direct path lookups returned "not found"; the only proofpunk-adjacent directories present (`proofpunk-doctrine/`, `truth-forge-doctrine/`, two `_retired-*` dirs) use different names or are explicitly retired.

### `~/.claude/plugins/`
- Plugin IS installed via marketplace: `~/.claude/plugins/installed_plugins.json` → key `plugins["proofpunk@proofpunk-marketplace"][0].installPath` = `/Users/nick/.claude/plugins/cache/proofpunk-marketplace/proofpunk/2.2.0`
- **Installed marketplace version = 2.2.0** (embedded in the install path itself, confirming the manifest-string finding above).
- `~/.claude/plugins/data/` has TWO proofpunk-related cache entries: `proofpunk-proofpunk-marketplace/` (4d old) and `proofpunk-inline/` (1w old). Directly listed both: **both are EMPTY directories** (0 files). This is consistent with the pattern seen across every other plugin listed in `~/.claude/plugins/data/` (a generic per-plugin data-cache scratch dir that Claude Code creates for every installed/enabled plugin regardless of whether that plugin uses it — e.g. `railway-claude-plugins-official/`, `pdf-viewer-inline/`, etc. are also empty). Neither entry contains a manifest, hook registration, or any file; **this fully resolves the earlier open question — there is no second hook-registration path hiding in `proofpunk-inline/`.**
- `~/.claude/settings.json` confirms only ONE proofpunk marketplace source is registered: `marketplaces.proofpunk-marketplace.source` = `{"source":"directory","path":"/Users/nick/proofpunk"}` (this repo checkout), and `plugins["proofpunk@proofpunk-marketplace"] = true` (enabled). No second `proofpunk*` marketplace registration found in this file. `proofpunk-inline/` under `plugins/data/` is a cache/data directory, not the enablement registry — its presence does not by itself prove a second ACTIVE install; the enablement registry (`installed_plugins.json` + `settings.json`) shows exactly one.
- **No duplicate proofpunk hook registration found in `~/.claude/settings.json`**: grepping this file for "proofpunk" returns only the enablement line and the marketplace-source block — no separate inline `hooks` object referencing proofpunk hook scripts. `~/.claude/settings.local.json` does not exist (glob: no match). No project-level `.claude/settings.json` or `.claude/settings.local.json` exists in this repo checkout (glob for `**/settings.json` and `**/settings.local.json` across the whole repo returned zero files). `proofpunk-inline/` (under `~/.claude/plugins/data/`) is confirmed EMPTY — no manifest, no hooks — so it carries no second registration. **The documented f95ba9d dual-hook-registration defect does NOT currently recur** on this machine.

### `~/.omp/agent/skills/` and `~/.omp/plugins/`
- **`~/.omp/agent/skills/` has ALL 18 canonical proofpunk skill names installed as LIVE standalone directories**, most recently modified 3 days ago: `visual-inspection`, `validation-plan`, `ui-experience-audit`, `tui-testing`, `stack-testing`, `session-intent`, `root-cause-debugging`, `red-team-eval`, `proofpunk`, `prompt-forge`, `production-readiness`, `plan-hardening`, `mobile-validation-runner`, `implement`, `full-functional-audit`, `end-user-testing`, `codebase-truth-audit`, `brainstorm`. This is the OPPOSITE result from `~/.claude/skills/`.
- Dozens of timestamped backup copies of the same 18 names exist as `.name.bak-YYYYMMDD-HHMMSS/` dotfile-prefixed dirs, with timestamps at least as old as 2026-08-11 and re-occurring through 2026-09-01 — proving this standalone-copy directory has been repeatedly re-synced (likely by an install/update script snapshotting before overwrite) for roughly a month.
- **Diff of `~/.omp/agent/skills/implement/SKILL.md` vs repo's `plugins/proofpunk/skills/implement/SKILL.md`** (both 245 lines, matching the orchestrator's hash-level finding): the ONLY difference is two citation paths —
  - repo: `../../references/platform-routing.md` and `../../references/api-validation.md`
  - omp standalone copy: `references/platform-routing.md` and `references/api-validation.md`
  This is exactly the installer's documented citation-rewrite behavior (per `AGENTS.md:27`: "`../../references/X` citations are deliberate — the installer rewrites them to self-contained copies"). **The OMP standalone copy is NOT drifted/stale content — it is the installed (rewritten) form of the current repo source**, differing only in the intentional post-install path transform. Confirms the installer ran successfully against this exact repo content.
- `~/.omp/plugins/installed_plugins.json` (read in full): key `plugins["proofpunk@proofpunk"][0]` = `{installPath: "/Users/nick/.omp/plugins/cache/plugins/proofpunk___proofpunk___2.2.0", version: "2.2.0", installedAt/lastUpdated: 2026-09-03T09:02:43Z}`. **OMP-registered version = 2.2.0**, matching Claude's cache-path finding and the repo's un-bumped `package.json`. Only ONE `proofpunk` entry exists in this file (plus unrelated `claude-crew` and a DISABLED `truth-forge@truth-forge` — the pre-rename predecessor plugin, `enabled: false`, confirming truth-forge is retired on the OMP side too, not a live duplicate).
- `~/.omp/plugins/cache/marketplaces/proofpunk/marketplace.json` (read in full): `metadata.version` = **"2.2.0"**, `plugins[0].version` = **"2.2.0"**. This is a SEPARATE, staler marketplace-index snapshot (checked out 4 days before this scan) from the actual installed-plugin cache dir `~/.omp/plugins/cache/plugins/proofpunk___proofpunk___2.2.0/` — both report the same version 2.2.0, consistent with the `installed_plugins.json` entry above. No version skew between the two OMP cache locations.

### `~/.config/opencode/` — CORRECTION: proofpunk IS installed here (earlier draft was WRONG)
A direct `glob` of `~/.config/opencode/{plugin,commands,agents,skills}` (not a text grep, which apparently missed it) shows proofpunk fully installed:
- `~/.config/opencode/plugin/proofpunk.ts` — EXISTS. Byte-diffed against repo `plugins/proofpunk/opencode/plugin/proofpunk.ts`: **0 differences, byte-identical.**
- `~/.config/opencode/commands/` — 6 files: `proofpunk-truth-audit.md`, `proofpunk-verify.md`, `proofpunk-implement.md`, `proofpunk-install.md`, `proofpunk-rate-prompt.md`, `proofpunk-forge-prompt.md`. Matches repo `opencode/commands/*` 1:1 by name.
- `~/.config/opencode/agents/proofpunk.md` — EXISTS. Byte-diffed against repo `plugins/proofpunk/opencode/agents/proofpunk.md`: **0 differences, byte-identical.**
- `~/.config/opencode/skills/proofpunk` — RESOLVED: this is a single FILE (`SKILL.md`, 5.5KB), not a directory containing all 18 skills. It is the router skill only, analogous to `plugins/proofpunk/skills/proofpunk/SKILL.md`. `~/.config/opencode/skills/proofpunk-doctrine/` also exists as a separate, differently-named legacy entry (same unrelated bundle seen under `~/.claude/skills/proofpunk-doctrine/`).
- **Therefore for OpenCode: only the router skill (`proofpunk`) is installed standalone under `~/.config/opencode/skills/`; the other 17 delivery skills (implement, brainstorm, visual-inspection, etc.) are NOT present as standalone OpenCode skill files.** A bare invocation of the router (`proofpunk`) resolves to this standalone file; a bare invocation of any of the other 17 canonical skill names would NOT resolve via `~/.config/opencode/skills/` and must come from wherever OpenCode's plugin-skill loading mechanism reads `plugins/proofpunk/skills/*` (not independently verified in this pass — OpenCode's skill-resolution search path was not inspected).
- **Answer: proofpunk IS installed under `~/.config/opencode/`, and the plugin.ts + router agent are byte-identical to the repo's current OpenCode-specific sources.** The earlier "No matches found" conclusion was a false negative from a text-grep across a directory tree (the `grep` tool searches file contents by default, not names, and evidently missed or mis-scoped this directory) — corrected by direct `glob` + `diff`.

### `~/.claude/settings.json` — full hook-registration content
Read in full (~250 lines of the hooks block). It DOES contain a large `hooks` object, but it is a **generic PASEO/orca hook framework wrapper** (`Notification`, `PermissionRequest`, `PostToolUse`, `PostToolUseFailure`, `PreToolUse`, `SessionEnd`, `SessionStart`, `Stop`, `StopFailure`, `SubagentStart`, `SubagentStop`, `TeammateIdle`, `UserPromptSubmit`) whose commands invoke `/Users/nick/.orca/agent-hooks/claude-hook.sh` and `$PASEO_HOOK_CLI` — **none of these command strings reference `stop-guard.sh`, `evidence-guard.sh`, `capture-guard.sh`, `no-test-files.sh`, `post-write-walkthrough.sh`, `session-start.sh`, `instructions-loaded.sh`, `bash-write-notice.sh`, or `bash-write-snapshot.sh` by name.** Confirmed via targeted grep of those 9 script basenames against `~/.claude/settings.json`: zero matches. Proofpunk's own 9 hook scripts are registered exclusively through the plugin-loading mechanism (`plugins["proofpunk@proofpunk-marketplace"]: true` + the plugin's own `hooks/hooks.json`), not duplicated inline in `settings.json`.

### Marketplace cache version
- `~/.claude/plugins/cache/proofpunk-marketplace/proofpunk/2.2.0/` — the directory name itself encodes the pinned version: **2.2.0**, consistent with the repo's un-bumped `package.json`.

### Are there two installed versions both registering hooks? (f95ba9d historical defect)
- Searched every settings.json reachable in this scan: `~/.claude/settings.json` (found, grepped for "proofpunk"), `~/.claude/settings.local.json` (does not exist), repo-tree `**/settings.json` / `**/settings.local.json` (zero matches).
- **Result: no duplicate hook registration found in the settings files actually present.** `~/.claude/settings.json` shows exactly one proofpunk marketplace registration (source path `/Users/nick/proofpunk`) and one enablement flag (`true`). No separate inline `hooks` block duplicating `plugins/proofpunk/hooks/hooks.json` was found anywhere reachable.
- `~/.claude/plugins/data/proofpunk-inline/` and `~/.claude/plugins/data/proofpunk-proofpunk-marketplace/` were both directly listed and confirmed EMPTY (0 files each) — consistent with every other plugin's per-plugin data-cache scratch dir in this same directory, not a second install with its own hook registration.
- **DEFINITIVE ANSWER: there is exactly ONE live hook registration path for proofpunk on this machine** — the plugin-loading mechanism (`plugins["proofpunk@proofpunk-marketplace"]: true` in `~/.claude/settings.json` → reads `plugins/proofpunk/hooks/hooks.json`, 11 registrations across 7 events). No duplicate registration exists in `~/.claude/settings.json`'s own inline `hooks` block (that block is an unrelated generic PASEO/orca framework wrapper, script-checked to contain none of proofpunk's 9 script basenames), and the two candidate "second install" cache dirs (`proofpunk-inline/`, `proofpunk-proofpunk-marketplace/`) are both empty. **The historical f95ba9d dual-registration defect is NOT currently reproduced** on this machine as of this scan.

## Open Questions
1. Whether `proofpunk-doctrine/` and `truth-forge-doctrine/` under `~/.claude/skills/` (and their `~/.omp/agent/skills/` counterparts) should be cleaned up is a judgment call outside this lane's scope (D7 asked "would a bare invocation resolve to plugin or standalone copy" — answered — not "should these leftover dirs be deleted").
2. OpenCode's plugin-skill resolution mechanism for the 17 non-router delivery skills (not present as standalone files under `~/.config/opencode/skills/`) was not independently traced — unclear whether OpenCode reads `plugins/proofpunk/skills/*` directly at plugin-load time or via another indirection. Does not affect the D7 acceptance answers, which are all otherwise fully resolved with direct evidence.
