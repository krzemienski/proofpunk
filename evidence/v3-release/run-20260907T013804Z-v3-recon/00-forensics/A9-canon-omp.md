# A9 — OMP skill loader contract + Codex/AGENTS.md surface (C4, C5)

STATUS: COMPLETE

## Summary

- OMP 18.1.11 installed at `/Users/nick/.bun/bin/omp` → symlink → `/Users/nick/node_modules/@oh-my-pi/pi-coding-agent/dist/cli.js`. Full TypeScript source tree is readable (unbundled) at `/Users/nick/node_modules/@oh-my-pi/pi-coding-agent/src/`.
- Skill capability has **9 registered providers**, not the "3 ways" the brief framed exposure as (those 3 are exposure *mechanisms*, not source count). Measured priority order (higher wins on name collision): `native`=100, `omp-plugins`=90, `claude`=80, `agent-plugins`=75, then a 70-way tie between `claude-plugins`, `agents`, `codex`, then `opencode`=55, `github`=30, `managed-skills`=5 (lowest — auto-learn).
- `~/.omp/agent/skills/` (native, user level, priority 100) and `~/.omp/agent/managed-skills/` (priority 5, lowest) BOTH exist on this host and were listed via `ls`. Native user skills dir contains 27 dirs, all `.bak-*` backup timestamp dirs (not live skills) — see Open Questions.
- Native provider (`builtin.ts`) also does a project walk-up scanning `<ancestor>/.omp/skills/` from cwd to repo-root-or-home, closest-first, at the SAME priority=100 as user-level; project vs user tie-break within native provider is not yet isolated (see Open Questions).
- `docs/skill-canon.md` in THIS repo already contains an extensive, citation [11]-sourced OMP comparison table, built from `omp://skills.md` doc text — that prior work self-reports it did NOT verify OMP behavior against OMP's own source code (see its finding #4, lines 496-508). This lane's source-level read supersedes/corroborates that gap.
- `skill://<name>` protocol handler (`skill-protocol.ts`) resolves to `SKILL.md` by default, or a relative sub-path with strict traversal guards (no `..`, no absolute, and a `containRoot` canonicalization check for Agent-Plugin-sourced skills).
- Codex provider (`codex.ts`) loads AGENTS.md at **user level only** — `~/.codex/AGENTS.md` — via full auto-discovery, no explicit per-skill listing found in OMP's Codex provider. Project-level Codex AGENTS.md is NOT loaded by this provider (only user-level, per source comment line 5 and code lines 58-59). This contradicts part of the brief's premise that skills "need EXPLICIT listing in AGENTS.md" — for OMP's Codex provider, that's not what's happening; it's auto-discovery of one file at one path.
- `omp plugin doctor` and `omp plugin list` are read-only introspection commands; `omp plugin list` confirms `proofpunk@proofpunk` is installed at v2.2.0 (STALE relative to git HEAD's manifest claims — corroborates orchestrator's stated F-00x driftr) as a **plugin marketplace install**, not the skills-only install path.
- proofpunk's own README documents 4 distinct install targets: Claude Code (`/plugin install`), OMP (`omp plugin install`), OpenCode (installer script or full plugin), and "any agent" (`tools/proofpunk-install.sh --target {claude-code|omp|opencode|agents}`). All 4 are drivable on this host in principle; OMP path is the one actually installed and verifiable live.

## Findings

| ID | Finding | Evidence (path:line or command) | Confidence |
|---|---|---|---|
| A9-01 | OMP 18.1.11 resolves to a real, non-minified TS source tree | `omp --version` → `omp/18.1.11`; `realpath /Users/nick/.bun/bin/omp` → `/Users/nick/node_modules/@oh-my-pi/pi-coding-agent/dist/cli.js`; `src/` dir readable with normal `read` tool (not bundled/minified) | high |
| A9-02 | Skill capability has 9 registered providers across the discovery tree, not "3 exposure ways" | `grep_ide` pattern `registerProvider.{0,3}(Skill\|skillCapability)` over `src/discovery/` → hits in `agent-plugins.ts:327`, `agents.ts:189`, `builtin.ts:323,331`, `claude-plugins.ts:602`, `claude.ts:536`, `codex.ts:499`, `github.ts:323`, `omp-plugins.ts:355`, `opencode.ts:433` | high |
| A9-03 | Measured priority order for skill-name-collision resolution | Read each provider's `PRIORITY` const: `builtin.ts:42` native=100; `builtin.ts:313` managed=5; `omp-plugins.ts:46` =90; `claude.ts:35` =80; `agent-plugins.ts:41` =75 ("Above claude-plugins (70) so the standard's semantics win... below claude.ts (80)"); `claude-plugins.ts:32` =70 ("Below claude.ts (80) so user .claude/ overrides win"); `agents.ts:28` =70; `codex.ts:44` =70; `opencode.ts:45` =55; `github.ts:42` =30 | high |
| A9-04 | Native provider skill roots: user-level `~/.omp/agent/skills/` + project walk-up `<ancestor>/.omp/skills/` from cwd to repo-root-or-home | `src/extensibility/skills.ts` is a re-export layer; actual scan logic is `builtin.ts:282-307` `loadSkills()`: `ancestors.map(...scanSkillsFromDir({dir: path.join(dir, PATHS.projectDir, "skills")...}))` for project, plus one `scanSkillsFromDir({dir: path.join(getAgentDir(), "skills")...})` for user | high |
| A9-05 | Managed-skills (auto-learn) is a SEPARATE lowest-priority (5) provider reading `~/.omp/agent/managed-skills/`, by design so any authored skill of the same name from ANY other provider wins | `builtin.ts:309-321` comment + `MANAGED_SKILLS_PRIORITY = 5` + `loadManagedSkills()` calling `scanSkillsFromDir({dir: getManagedSkillsDir()...})` | high |
| A9-06 | On this host, `~/.omp/agent/skills/` (native, prio 100) contains 27 entries, ALL timestamped `.bak-*` backup dirs (e.g. `.brainstorm.bak-20260811-102208`), not live skill names | `ls -la /Users/nick/.omp/agent/skills/` → 27 dirs all matching `.<name>.bak-<timestamp>` pattern | high — but see A9-OQ-1 for whether these are excluded by dot-prefix filtering |
| A9-07 | `~/.omp/agent/managed-skills/` on this host contains many live-looking skill dirs (e.g. `9router-cli-tool-isolated-fv`, `absence-evidence-capture`, `audit-build-prompt`) — no dot-prefix, no `.bak` suffix | `ls -la /Users/nick/.omp/agent/managed-skills/` → dozens of plain-named dirs | high |
| A9-08 | `skill://<name>` resolves via `getActiveSkills()` (process-global snapshot set once per session) to `SKILL.md` (default) or a validated relative sub-path within `skill.baseDir`, with 3 layers of traversal defense: string-level `..` check, `path.resolve` prefix check, and (for Agent-Plugin sourced skills only) a `resolveContainedPath` canonical-path re-check against `containRoot` | `skill-protocol.ts:28-42` (`validateRelativePath`), `:66-95` (resolve logic + `containRoot` branch) | high |
| A9-09 | A discovered `Skill` interface (per `skills.ts:18-37`, elided in first read — not yet re-read in full) exposes at minimum `name`, `description`, `filePath`, `baseDir`, and (conditionally) `containRoot`, `frontmatter`, `_source` — confirmed via usages in `builtin.ts` scan-result mapping (`skillMap.set(capSkill.name, {..., filePath: capSkill.path, baseDir: ..., source: ...})`) and `skill-protocol.ts` (`skill.baseDir`, `skill.filePath`, `skill.containRoot`) | `builtin.ts` OCR'd frame around lines 106-115 (via image, not text-read — flagging as partially OCR-sourced); `skill-protocol.ts:73,83,94` (`skill.baseDir`, `skill.containRoot`, `skill.filePath`) | med (interface body itself not yet re-read verbatim, only inferred from call sites + one image-OCR pass) |
| A9-10 | Slash-command exposure: `/skill:<name>` computed as `skill:${skill.name}` — confirms brief's mechanism (c) is real and named exactly this way | `skills.ts:408-410` `getSkillSlashCommandName` | high |
| A9-11 | System-prompt injection is name+description metadata (mechanism a) — not yet located the exact injection call site/gating condition; `docs/skill-canon.md:147` (prior work, NOT independently re-verified by this lane) claims injection is conditional on "if `read` tool is available" | `docs/skill-canon.md:147` cites `omp://skills.md`, a DOC not the source. This lane has not yet independently confirmed that conditional in `skills.ts` or the prompt-assembly code | low — flagged, not yet source-verified |
| A9-12 | Codex provider (OMP's Codex compatibility surface) loads AGENTS.md ONLY at `~/.codex/AGENTS.md` (user level) — explicitly no project-level Codex AGENTS.md load path in this provider | `codex.ts:5` header comment "System Instructions: AGENTS.md (user-level only at ~/.codex/AGENTS.md)"; `codex.ts:54-71` `loadContextFiles()` body reads exactly one path, `path.join(ctx.home, SOURCE_PATHS.codex.userBase, "AGENTS.md")`, no project-dir variant | high |
| A9-13 | Codex provider's skill loader (`codex.ts:237+`, `loadSkills`) scans BOTH `~/.codex/skills` (user) and `<cwd>/.codex/skills` (project) — full auto-discovery, no per-skill explicit listing found in the OMP Codex provider code read so far | `codex.ts:237-240` (`userSkillsDir`, `projectSkillsDir` both computed and presumably both scanned — body past line 240 not yet fully read) | med — function body past line ~240 not yet read in full |
| A9-14 | OMP's OWN top-level `AGENTS.md` provider (distinct from the Codex-specific one) is a project-only, home-directory-boundary-aware upward walk from cwd, EXCLUDING the home dir's own AGENTS.md as project context when the repo is nested under home | `agents-md.ts:42-97` full `loadAgentsMd()` body read verbatim | high |
| A9-15 | This is NOT the same file/path as `codex.ts`'s AGENTS.md loader — OMP has (at least) two separate AGENTS.md-reading code paths: a generic project-root-walking one (`discovery/agents-md.ts`, provider id `agents-md`) and a Codex-specific user-level-only one (`discovery/codex.ts`, provider id `codex`) | Compare `agents-md.ts:15` (`PROVIDER_ID = "agents-md"`) vs `codex.ts:42-43` (`PROVIDER_ID = "codex"`) — two distinct registered providers both touching AGENTS.md semantics but with different scope rules | high |
| A9-16 | `omp plugin list` (read-only) confirms `proofpunk@proofpunk (2.2.0) (user)` is installed as a marketplace plugin, alongside `claude-crew@claude-community (2.0.4)` and `truth-forge@truth-forge (1.7.0)` | `bash: omp plugin list` output | high |
| A9-17 | `omp plugin doctor proofpunk` reports: plugins_directory found, package_manifest "Not created yet" (warning), node_modules found — 2 ok, 1 warning, 0 errors | `bash: omp plugin doctor proofpunk` output | high |
| A9-18 | `omp plugin features proofpunk` reports "No optional features available" | `bash: omp plugin features proofpunk` output | high |
| A9-19 | `/Users/nick/.omp/plugins/installed_plugins.json` confirms proofpunk install path `/Users/nick/.omp/plugins/cache/plugins/proofpunk___proofpunk___2.2.0`, installed 2026-09-03T09:02:43.980Z, scope "user", and — unlike the other two plugins — has NO `enabled` key at all (both others have explicit `enabled: true`/`false`) | `installed_plugins.json:24-32` read verbatim | high |
| A9-20 | proofpunk README documents 4 install surfaces verbatim: Claude Code (`/plugin marketplace add` + `/plugin install`), OMP (`omp plugin marketplace add` + `omp plugin install`), OpenCode (installer script OR full plugin via same catalog), "Any agent" (`tools/proofpunk-install.sh --target {claude-code,omp,opencode,agents}`) | `README.md:8-34` read verbatim | high |
| A9-21 | proofpunk's own `docs/skill-canon.md` already did extensive Codex/OpenCode/Claude/OMP comparison work (citations [1]-[12]) BUT explicitly flags in its own "Open Questions"/limitations section (lines 496-508) that it did NOT verify OMP against OMP's own source code — only against `omp://skills.md` doc text read via the `read` tool | `docs/skill-canon.md:496-508` read verbatim via grep context | high |
| A9-22 | `docs/skill-canon.md:138` prior-work claim of priority order `native=100, omp-plugins=90, claude=80, {claude-plugins, agents, codex}=70, opencode=55, github=30, omp-managed=5` — this lane's independent source read CONFIRMS these exact 7 numbers but adds `agent-plugins=75` as an 8th provider the prior doc's table appears to have omitted or folded in elsewhere | Compare `docs/skill-canon.md:138` (7 providers listed) against this lane's A9-03 (8 distinct provider IDs with numeric priority, 9 total registrations counting the 2 in `builtin.ts`) | med — the prior doc's own citation [11] is `omp://skills.md`, a doc; this lane's numbers come from `src/discovery/*.ts` directly, so where they disagree this lane's source-level reading should be treated as more authoritative, but full reconciliation not yet done |

## Open questions

- **A9-OQ-1**: Whether OMP's `scanSkillsFromDir` helper (in `src/discovery/helpers.ts`, not yet read) filters out dot-prefixed directories (`.brainstorm.bak-*`) by convention, which would explain why `~/.omp/agent/skills/` (27 `.bak-*` dirs, all dot-prefixed) appears to have effectively ZERO live native user-level skills on this host despite being the highest-priority (100) provider. Tried: `ls -la` on the dir; have NOT yet read `helpers.ts`'s `scanSkillsFromDir` glob pattern to confirm dot-exclusion.
- **A9-OQ-2**: Whether project-level native skills (`<ancestor>/.omp/skills/`) exist anywhere in the ancestor chain from `/Users/nick/proofpunk` up to `/Users/nick` (home) or further. Not yet globbed.
- **A9-OQ-3**: Tie-break rule when native project-level and native user-level both match the same skill name (both priority=100, same provider, two separate scan calls merged via `Promise.all` + `.flatMap`) — is it array-order-first-wins, or is there a level-aware sort? Not yet read past `builtin.ts:307`; the general capability-level dedup logic lives elsewhere (possibly `src/capability/*` or a `loadCapability` function referenced in an OCR'd frame from `skills.ts` — not independently re-verified via text read).
- **A9-OQ-4**: The exact `Skill` interface field list (`skills.ts:18-37`) — only read via one image OCR frame, not a clean text read. Should re-read with `:2-37` to get a verbatim text render before final citation.
- **A9-OQ-5**: Where system-prompt injection of skill name+description actually happens (gating condition, truncation length, whether conditional on `read` tool availability as `docs/skill-canon.md:147` claims) — not yet located in source by this lane.
- **A9-OQ-6**: Full body of `codex.ts` `loadSkills()` past line ~240, to confirm whether Codex's OMP provider does anything resembling "explicit listing" (the brief's specific C5 question) versus pure auto-discovery. Partial read only.
- **A9-OQ-7**: proofpunk's `docs/skill-canon.md` claim that OMP's discovered-skill system-prompt listing is itself conditional ("if `read` tool is available") is sourced ONLY from `omp://skills.md` (a doc, not source) per that document's own citation [11] — this lane has not independently confirmed or refuted this from `src/`.
- **A9-OQ-8**: Whether `agentskills.io`'s "40+ supporting tools" / `skills` CLI claim (from the work-order brief, not yet found verbatim in this repo's own docs) is referenced anywhere in proofpunk's own README/AGENTS/docs — grep for "agentskills.io" only found it in `docs/skill-canon.md`'s citation list (as [1], the spec URL itself), not any "40+ tools" or "skills CLI" language. Not yet confirmed whether that specific claim originates from proofpunk's docs at all, or was brief-external context. **Flagging as a likely case where the brief's premise ("the open standard... claims 40+ supporting tools") is external to this repo** and not something proofpunk itself asserts — no proofpunk platform-support claim needs to be checked against it, since proofpunk doesn't make that specific 40+ claim itself.
- **A9-OQ-9**: Have not yet verified WHICH of proofpunk's 4 stated install-target platforms are actually drivable/testable on THIS machine right now (e.g. is `~/.config/opencode/` present? is opencode itself installed as a binary?). Started but interrupted before checking.

## Addendum — resolved open questions

- **A9-OQ-1 RESOLVED**: `scanSkillsFromDir` (`src/discovery/helpers.ts:426-427`) skips any directory entry whose name starts with `.` (`if (entry.name.startsWith(".")) continue;`) BEFORE looking for `SKILL.md`. This is the SAME helper used by every provider (native, codex, opencode, etc.) — dot-prefix skip is global, not native-specific. Confirms: `~/.omp/agent/skills/`'s 27 `.bak-*` dirs are ALL silently excluded from discovery. Practically, the highest-priority native user-level skill root on this host currently contributes **zero** live skills. Evidence: `helpers.ts:426-433` read verbatim.
- **A9-OQ-4 RESOLVED**: `Skill` interface (`extensibility/skills.ts:18-37`) verbatim: `{ name: string; description: string; filePath: string; baseDir: string; source: string; hide?: boolean; containRoot?: string; _source?: SourceMeta }`. Matches the brief's claimed fields (`name`, `description`, `filePath`, `baseDir`, source metadata) exactly, plus two extras (`hide`, `containRoot`) not mentioned in the brief. `hide` is explicitly documented inline (lines 24-29): skill stays loaded/reachable via `skill://` and `/skill:` but excluded from the rendered `<skills>` system-prompt listing.
- **A9-OQ-6 RESOLVED**: Full `codex.ts` `loadSkills()` body (lines 237-259) confirmed: scans BOTH `~/.codex/skills` (user) and `<cwd>/.codex/skills` (project) via the shared `scanSkillsFromDir` helper — this IS pure auto-discovery, non-recursive, dot-skip applies. **No explicit per-skill listing mechanism found in OMP's Codex-compat skill provider.** This directly contradicts the brief's premise that "for the Codex/AGENTS.md surface, skills may need EXPLICIT listing in the governing AGENTS.md with full paths rather than auto-discovery" — at least for OMP's own Codex-compat provider, that premise does not hold. (Whether real upstream OpenAI Codex itself requires explicit listing is a separate question this lane did not investigate — `docs/skill-canon.md:146` claims real Codex's *system-prompt injection* is path-inclusive and budget-capped, which is a different claim than "must be explicitly listed in AGENTS.md".)
- **A9-OQ-8 RESOLVED (no further hits)**: Re-grepped `README.md`, `AGENTS.md`, `plugins/proofpunk/AGENTS.md`, `tools/INSTALL.md`, `docs/skill-canon.md` for `agentskills.io|40\+|skills CLI|npx skills` — only hit is the citation-list URL `docs/skill-canon.md:22` (the spec itself, not a "40+ tools" or "CLI" claim). **proofpunk itself never asserts the "40+ supporting tools" or generic `skills` CLI claim from the work-order brief.** That claim is external to this repo's own documentation; there is no proofpunk platform-support claim to reconcile against it.
- **A9-OQ-9 RESOLVED**: All three alternate agent CLIs referenced in proofpunk's README install matrix are present as real binaries on this host: `which opencode` → `/Users/nick/.bun/bin/opencode`; `which claude` → `/Users/nick/.local/bin/claude`; `omp` already confirmed. `~/.config/opencode/` exists and is a large, heavily-used real config directory (42.8K `AGENTS.md`, `opencode.json`, dozens of timestamped backups, `plugin/`, `plugins/`, `commands/`, `agents/` subdirs) — this is a live, actively-used OpenCode installation, not a stub. **All 4 of proofpunk's stated install targets (Claude Code, OMP, OpenCode, "any agent" script) are drivable on this machine in principle**; only the OMP path was independently confirmed as actually-installed-and-live via `omp plugin list` in this lane's scope. Whether Claude Code or OpenCode ALSO currently have proofpunk installed was not checked (out of this lane's C4/C5 scope — that's an installer/state-count concern for other lanes, e.g. A2/A4).

## Unresolved (genuinely out of scope or unreachable in this lane's time)

- **A9-OQ-2**: Ancestor-chain project-level native `.omp/skills/` directories were globbed for `/Users/nick/proofpunk/.omp/skills/**` and `/Users/nick/.omp/skills/**` — both "Path not found" (glob tool reports no matching path, i.e. neither directory exists on this host). So on this host, native project-level skills (priority 100, ancestor walk) also contribute zero skills, same as native user-level (OQ-1). Practically: of the two highest-priority OMP providers (native=100, omp-plugins=90), only `omp-plugins` (the proofpunk plugin install itself, via `~/.omp/plugins/cache/plugins/proofpunk___proofpunk___2.2.0`) is actually contributing live skills on this host right now.
- **A9-OQ-3**: Native provider's tie-break rule when BOTH project-level and user-level scans (same priority=100) return a same-named skill was NOT resolved — the capability-level dedup logic lives outside `builtin.ts`/`skills.ts` (likely `src/capability/index.ts` or similar, not read in this lane's time budget). Given OQ-2's finding that project-level native skills don't exist on this host at all, this tie-break is currently moot for this machine's actual state, but remains architecturally unverified.
- **A9-OQ-5**: System-prompt injection gating condition (whether truly conditional on "read tool availability" per `docs/skill-canon.md:147`'s doc-sourced claim) was NOT independently verified against source in this lane — time-boxed out. Flag for a follow-up lane if this specific claim needs source-level (not doc-level) confirmation.
- **A9-22 addendum**: `docs/skill-canon.md`'s prior-work table lists 7 numeric priorities and appears to have folded `agent-plugins` (75) into the narrative rather than the table row at line 138 — this lane did not fully reconcile whether that's an omission or an intentional simplification in the prior doc; flagging for the orchestrator's cross-lane synthesis rather than resolving unilaterally here.
