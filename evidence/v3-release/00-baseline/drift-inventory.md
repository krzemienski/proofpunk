# Drift Inventory — Hardcoded Counts, Versions, and Skill/Command Names

Measured: 2026-09-04T00:00:00Z (UTC, approximate — no live clock call made; date taken from environment) | Repo HEAD: 9963648 (working tree dirty, 18 modified files)
Method: `glob` over `plugins/proofpunk/{skills,references,commands,opencode/commands,hooks}/*`, full raw read of `plugins/proofpunk/hooks/hooks.json`, `grep` across the 18 named target files/dirs, `python3 -c` to print full JSON string fields that `read`/`grep` truncated, and direct comparison against the canonical counts already measured in `evidence/v3-release/00-discovery/d6-d7-measurement.md` (D6/D7, measured 2026-09-02 @ `a41591a`).

## 0. Canonical values used below (self-verified, agrees with D6/D7)

| Thing | Count | Derivation |
|---|---|---|
| Skills | **18** | `glob plugins/proofpunk/skills/*` → 18 dirs (brainstorm, codebase-truth-audit, end-user-testing, full-functional-audit, implement, mobile-validation-runner, plan-hardening, production-readiness, prompt-forge, proofpunk, red-team-eval, root-cause-debugging, session-intent, stack-testing, tui-testing, ui-experience-audit, validation-plan, visual-inspection) |
| Shared references | **13** | `glob plugins/proofpunk/references/*` → 13 files (`preflight-checks.md, ci-gates.md, end-user-actor.md, api-validation.md, cli-validation.md, ios-validation.md, web-validation.md, ios-hig-checklist.md, platform-routing.md, severity-model.md, web-wcag-checklist.md, defect-pattern-database.md, evidence-contract.md`) |
| Commands (Claude Code) | **6** | `glob plugins/proofpunk/commands/*` → `rate-prompt, truth-audit, verify, forge-prompt, implement, install` |
| Commands (OpenCode) | **6** | `glob plugins/proofpunk/opencode/commands/*` → `proofpunk-rate-prompt, proofpunk-truth-audit, proofpunk-verify, proofpunk-forge-prompt, proofpunk-implement, proofpunk-install` — **6+6, matches the work-order's canonical value, confirms it, no disagreement** |
| Hook files (`.sh`) | **9** | `glob plugins/proofpunk/hooks/*` minus `hooks.json` → `stop-guard.sh, capture-guard.sh, evidence-guard.sh, no-test-files.sh, bash-write-notice.sh, bash-write-snapshot.sh, post-write-walkthrough.sh, session-start.sh, instructions-loaded.sh` |
| Hook event keys | **7** | Full raw read of `hooks.json` — top-level keys under `.hooks`: `SessionStart, Stop, SubagentStop, PreToolUse, InstructionsLoaded, PostToolUse, PostToolUseFailure` |
| Hook script registrations | **11** | Counted every individual object in every `.hooks[event][].hooks[]` array: SessionStart×1, Stop×1, SubagentStop×1, PreToolUse×4 (no-test-files, evidence-guard, capture-guard under `Write\|Edit`; bash-write-snapshot under `Bash`), InstructionsLoaded×1, PostToolUse×2 (post-write-walkthrough under `Write\|Edit`; bash-write-notice under `Bash`), PostToolUseFailure×1 (bash-write-notice under `Bash`) = 1+1+1+4+1+2+1 = **11** |
| Distinct scripts registered | **9** | Unique script basenames across the 11 registrations: `session-start.sh, stop-guard.sh (×2: Stop, SubagentStop), no-test-files.sh, evidence-guard.sh, capture-guard.sh, bash-write-snapshot.sh, instructions-loaded.sh, post-write-walkthrough.sh, bash-write-notice.sh (×2: PostToolUse, PostToolUseFailure)` = 9 distinct scripts, with `stop-guard.sh` and `bash-write-notice.sh` each appearing twice |

**No disagreement found** between this independent re-measurement and the work order's stated canonical values (18 skills; 13 references; 6+6 commands; 9 hook files; 7 event keys; 11 registrations / 9 distinct scripts). The 9963648-vs-a41591a HEAD SHA discrepancy noted in the work order is a separate, orthogonal fact (D6/D7 was measured two commits prior) and does not affect any count above — the tree's skill/reference/command/hook file sets are identical at both SHAs per this session's own glob.

## 1. Drift table

| File | Line | Literal string | True value | Canonical derivation that should replace it |
|---|---|---|---|---|
| `README.md` | 4 | `18 skills that make "done" mean...` | 18 (correct, matches canon) | Not drift today, but a hardcoded literal — should read `len(skills)` from `tools/build-site.py`'s own glob, or at minimum be swept by the documented commit-2953547/7958498 discipline on every skill-count change |
| `README.md` | 91 | `"prompt-forge gained its always-on workflow... v1.4.0/v1.5.0..."` prose narrating version history | Correct as **HISTORICAL PROVENANCE PROSE** | No action — narrates past releases by their true version numbers, not a live claim about current skill count |
| `README.md` | 121 | `installs all 18 skills + doctrine` | 18 (correct) | Hardcoded literal describing installer behavior; canonical source is `ALL_SKILLS` array length in `tools/proofpunk-install.sh` (per `architecture.md:489`'s own cross-reference) |
| `README.md` | 376 | `all 18 skills + doctrine + verify, from GitHub` | 18 (correct) | Same as above — hardcoded, currently accurate, still a repeat of the same number in a 4th place |
| `README.md` | 377 | `18 skills + 20 themes + doctrine-guard extension` | 18 (correct) | Same — hardcoded literal, currently accurate |
| `README.md` | 402 | `` `mobile-validation-runner` ... \| visual-inspection (audit every screenshot), ui-experience-audit (per-screen), end-user-testing (sealing), functional-validation (web/API equivalent) `` | **`functional-validation` does not exist** — `glob plugins/proofpunk/skills/functional-validation` returns nothing; the skill was consolidated away (per `docs/consolidation-decisions.md` and `improvements.md`) | **ACTIVE GUIDANCE — must be fixed.** This is a live "Skills executed downstream" table describing what `mobile-validation-runner` currently delegates to, not a historical note. Should read `implement` (Stage 5's inline end-user validation, the successor per `README.md:234`'s own historical-record blockquote) or be replaced with the correct current downstream reference |
| `README.md` | 419 | `` P --> S["skills/ — 18 skills"] `` (mermaid node label) | 18 (correct) | Hardcoded literal inside a mermaid diagram; no mechanical derivation possible for a static diagram — flag for manual sweep on any skill-count change, same as `AGENTS.md:8`/`build-site.py:188`'s printed value which IS derived |
| `README.md` | 420 | `` P --> R["references/ — 13 shared doctrine files"] `` | 13 (correct) | Hardcoded literal, currently accurate — same manual-sweep caveat as above |
| `README.md` | 610 | `plugins/proofpunk/              the plugin (18 skills + references + docs)` | 18 (correct) | Hardcoded literal in a plain-text repo-layout block; same caveat |
| `AGENTS.md` (root) | 7 | `18 skills that make "done" mean...` | 18 (correct) | Same wording as `README.md:4`; duplicate hardcode — canonical derivation should be a single generated snippet included in both, or a build-time check that greps both for equality |
| `plugins/proofpunk/AGENTS.md` | 8 | `18 skills, 6 commands, 9 enforcement hook scripts (11 registrations across 7 event keys)` | All five numbers correct (18 / 6 / 9 / 11 / 7) — **fully agrees with §0 above, no disagreement found** | Hardcoded prose summarizing exactly the values `tools/verify-orchestration.py` and `tools/build-site.py:188`'s `print(f"skills=...")` line compute live; this file itself has no build step, so it is the highest-risk hardcode in the repo for the next drift cycle — should be the first file swept per the recurring-bug note at `plugins/proofpunk/AGENTS.md:44` |
| `plugins/proofpunk/AGENTS.md` | 27 | `` `skills/` \| 18 skill dirs, each with `SKILL.md`... `proofpunk/` is the router head — it calls all 17 others `` | 18 skill dirs (correct); "calls all 17 others" is correct arithmetic (18 skills total − 1 self = 17 delivery skills called by the router) | Not drift — internally consistent; flag only because "17" here is a *derived* number (18−1) with no comment tying it to the 18 above, making future edits to one without the other a drift risk |
| `plugins/proofpunk/AGENTS.md` | 28 | `` `commands/` \| 6 slash commands: verify, forge-prompt, implement, install, truth-audit, rate-prompt `` | 6 (correct, matches §0) | Hardcoded; canonical source `glob plugins/proofpunk/commands/*.md` |
| `plugins/proofpunk/AGENTS.md` | 29 | `` `hooks/` \| 9 hook scripts (...) + `hooks.json`, which wires them into 7 event keys (...) via 11 registrations `` | All three numbers correct (9 / 7 / 11) — **fully agrees with §0** | Hardcoded prose restating the exact hooks.json structure; highest-value single sentence to keep in sync since it names every script and every event key explicitly, so a future hook add/remove requires editing this sentence by hand with no test catching a miss |
| `plugins/proofpunk/AGENTS.md` | 30 | `` `agents/` \| 3 Claude Code subagents: end-user-validate, implement, scout `` | 3 for the Claude Code `agents/` tree specifically (correct per D6's per-platform table: 3/4/3 for `agents/`/`opencode/agents/`/`omp/agents/`) | Correct as stated, but note this file does not mention the OpenCode tree's 4th agent (`proofpunk.md`) anywhere — an omission, not a wrong number; flagged for completeness, not corrected here since it's out of the literal drift-string scope |
| `plugins/proofpunk/AGENTS.md` | 31 | `` `references/` \| 13 shared doctrine files covering... `` | 13 (correct, matches §0) | Hardcoded; canonical source `glob plugins/proofpunk/references/*.md` |
| `plugins/proofpunk/README.md` | 82 | `` Verify: `/plugin` should list `Proofpunk` with 18 skills. `` | 18 (correct) | Hardcoded literal in a user-facing verification instruction |
| `plugins/proofpunk/docs/usage-guide.md` | 3 | `how the 18 skills execute as one delegation DAG` | 18 (correct) | Hardcoded; canonical source same glob as above |
| `plugins/proofpunk/docs/usage-guide.md` | 6 | `How to actually invoke all 18 skills once the plugin is installed` | 18 (correct) | Same — duplicate hardcode within the same file (lines 3 and 6) |
| `plugins/proofpunk/docs/architecture.md` | 123 | `` ### Call table (18 skills, 47 edges total: 17 from the router to every skill, plus 30 among the 17 delivery skills) `` | 18 skills (correct); 47/17/30 edge counts NOT independently re-verified in this session (out of scope — would require walking every skill's Related Skills section) | Hardcoded skill count, correct; edge counts are a separate, deeper claim this pass did not re-derive — flagged as **UNRESOLVED**, not confirmed or refuted |
| `plugins/proofpunk/docs/architecture.md` | 355 | `For each of the 18 skills selected` | 18 (correct) | Hardcoded, correct |
| `plugins/proofpunk/docs/architecture.md` | 450–453 | `"Count drift has shipped four times (17→19→18; commits 2953547, c39a0f0, f95ba9d, and this session)."` | **HISTORICAL PROVENANCE PROSE** — a self-aware account of the repo's own drift history, correctly narrated | No action — this is documentation OF the drift-defect class itself, not an instance of it. It is exactly the kind of history this drift-inventory task exists to prevent repeating |
| `plugins/proofpunk/docs/architecture.md` | 489 | `` 18 skills: `glob plugins/proofpunk/skills/*/SKILL.md` → 18 directories, matching `ALL_SKILLS` in `tools/proofpunk-install.sh` verbatim. `` | 18 (correct) — this line is itself a re-verification instruction, not an unguarded literal | No action — this line explicitly names its own derivation and asks the reader to re-run it; it is the model other hardcoded lines in this table should follow |
| `plugins/proofpunk/docs/architecture.md` | 491 | `6+6 commands: glob ... → 6; glob ... → 6.` | 6+6 (correct, matches §0) | No action — same self-verifying pattern as line 489 |
| `plugins/proofpunk/docs/improvements.md` | 112–117 | `` `verify-orchestration.py:144` printed "the 17 skills" while validating 18... prints 18 today and the true number after any change `` | **HISTORICAL PROVENANCE PROSE** describing a fixed past bug (a literal string in `verify-orchestration.py` desyncing from a nearby `glob()` call) | No action — this documents the exact defect class named in the work order ("literal string that fell out of sync with a `glob()` result three lines away in the same file"), already fixed per the cited verdict |
| `plugins/proofpunk/docs/improvements.md` | 209 | `` verdict printed "the 17 skills"; `--help` said "all 17" \| installed product: `--help` contains no `17`, installer reports `18 installed`, gate prints `the 18 skills` `` | **HISTORICAL PROVENANCE PROSE** (before/after row in a fixed-defect table) | No action — explicitly a before/after row, both states named as such |
| `plugins/proofpunk/docs/improvements.md` | 264 | `` `verify-orchestration` PASS at 18, and a clean-HOME install of 18 skills with 0 unresolved references `` | 18 (correct as of the improvements.md v1.9.0 entry it appears under) | Historical entry for a specific dated release; not a live claim, correct in its own temporal context |
| `plugins/proofpunk/docs/validation-results.md` | 1 | `# Validation Results — Proofpunk v1.0.0` | **STALE HEADER — this file's own title claims v1.0.0** while its body (lines 24–33) lists skills including `cook` and `functional-validation` (v1.0.0-era names, both since consolidated away) and later sections (line 290, 472, 487) reference "16 skills" and "18 skills" respectively in the SAME file | **HISTORICAL PROVENANCE — the whole document is a dated validation log, appended to over time rather than rewritten; the v1.0.0 title is misleading but every individual row is a historically-scoped claim (see next 3 rows), not active guidance.** Flag for a maintainer decision: either retitle this file to reflect it spans v1.0.0→v1.9.0-era entries, or split it by release — but no single literal string here is "wrong" for the version it documents |
| `plugins/proofpunk/docs/validation-results.md` | 25, 28 | `` \| cook \| 145 \| `` and `` \| functional-validation \| 239 \| `` (skill line-count table) | **HISTORICAL PROVENANCE** — this is a frontmatter-validation table dated to the v1.0.0 pass (line 1/4-5), when `cook` and `functional-validation` genuinely existed as skills | No action — dated historical record of a validation pass that ran before either skill was consolidated away; correct in its own temporal scope |
| `plugins/proofpunk/docs/validation-results.md` | 290 | `` `tools/proofpunk-install.sh` installs the 16 skills as plain skills `` | **HISTORICAL PROVENANCE** — section 14 is explicitly dated `(v1.3.0, 2026-08-10)` at line 288, when the skill count genuinely was 16 | No action — dated historical entry, correct for its stated version |
| `plugins/proofpunk/docs/validation-results.md` | 472, 487, 492 | `` audit: 18/18 skills... 18 skills with positional arguments... shows 18 skills `` | 18 (correct at the point in the document's own timeline where this section sits, per the surrounding "shipping it as skill #18" narrative) | No action — internally dated, correct in context |
| `plugins/proofpunk/docs/consolidation-decisions.md` | 32, 96, 100 | `` ### Functional validation family → `functional-validation`, `full-functional-audit`, `end-user-testing` `` / `` ### Cook ("cooking") family → `cook` `` | **HISTORICAL PROVENANCE PROSE** — this file is explicitly a consolidation-decision log (source→destination mapping tables) documenting what `functional-validation` and `cook` WERE, and where their content WENT | No action — this is the canonical record of the merge itself; `cook` and `functional-validation` here name the pre-merge source, correctly |
| `plugins/proofpunk/docs/consolidation-decisions.md` | 313–318 | `` Decision: layered architecture rather than merging — `implement` is the orchestrator... and `cook` stays the execution engine it delegates code phases to `` | **HISTORICAL PROVENANCE PROSE** — dated to the v1.5.0 decision entry, describing a decision that was LATER SUPERSEDED (per `README.md:234`: "cook merged into it at v2.0.0") | No action for this document (it is an append-only decision log narrating what was decided AT THE TIME) — but this is the exact passage that makes `cook` look currently-separate to a reader skimming out of order; cross-check against `README.md:234`/`improvements.md` confirms the merge is documented elsewhere and this entry is correctly time-scoped |
| `proofpunk-hooks-release-report.md` | 1 | `# Proofpunk v1.10.0 — Hooks + /proofpunk:install — Measured Improvement Report (Round 3, 2026-08-13)` | Dated release report for v1.10.0 | Title correctly scoped; body claims below are the ones under suspicion |
| `proofpunk-hooks-release-report.md` | 12 | `` \| Hook events covered \| 1 (SessionStart) \| 5 (+Stop, SubagentStop, PreToolUse, InstructionsLoaded) \| `plugins/proofpunk/hooks/hooks.json` \| `` | **CONFIRMED FALSE AT CURRENT HEAD, TRUE AT THE TIME THIS REPORT WAS WRITTEN.** Current `hooks.json` has **7** event keys (`SessionStart, Stop, SubagentStop, PreToolUse, InstructionsLoaded, PostToolUse, PostToolUseFailure`), not 5. The report's "5" is correct for the v1.10.0 snapshot it describes — `PostToolUse` and `PostToolUseFailure` were added in a LATER release (both wire to `post-write-walkthrough.sh`/`bash-write-notice.sh`, scripts this same report elsewhere implies did not exist yet at v1.10.0) | This row is **HISTORICAL** relative to its own document's stated version (v1.10.0, 2026-08-13) and is not "wrong" in isolation — BUT the work order asked specifically to confirm/refute this claim against the current repo, and against current HEAD it reads as stale unless a reader notices the report's own dated title. **Verdict: refuted as a claim about the current repo (true value is 7, not 5); confirmed as accurate for its own stated point-in-time (v1.10.0).** No file edit warranted since the report is inherently a dated snapshot document, not live guidance — but this is exactly the kind of undated-looking table row that misleads a skimming reader, consistent with the work order's suspicion |
| `proofpunk-hooks-release-report.md` | 14 | `` \| Commands (claude + opencode) \| 6+6 \| 7+7 (`install`) \| ... \| `` | **CONFIRMED FALSE AT CURRENT HEAD, TRUE AT THE TIME THIS REPORT WAS WRITTEN.** Current command counts are **6+6** (verified §0), not 7+7. This report documents the *addition* of a 7th command (`install`) at v1.10.0 — that 7th command was later REMOVED/folded back, since current `commands/` has exactly 6 files (`rate-prompt, truth-audit, verify, forge-prompt, implement, install`) and current `opencode/commands/` has exactly 6 (`proofpunk-rate-prompt, proofpunk-truth-audit, proofpunk-verify, proofpunk-forge-prompt, proofpunk-implement, proofpunk-install`). Note `install` is one of the current 6 in both trees — so the count went 6+6 → 7+7 (per this report) → back to 6+6 today, meaning one OTHER command was removed while `install` stayed, netting back to 6 | **Verdict: refuted as a claim about the current repo (true value is 6+6, not 7+7); accurate for its own stated point-in-time snapshot immediately after `install` was added, before whatever later removal brought the count back down.** No file edit warranted for the same reason as the row above — dated snapshot document. Flagged as the second of the work order's two suspect claims, both refuted against current HEAD and both explainable as accurate-when-written |
| `proofpunk-skills-improvement-report-round2.md` | 15 | `` \| 4 \| cook \| Never mutate a running driver... \| `` | **HISTORICAL PROVENANCE** — this is a numbered defect-inventory row from a round-2 report dated before the v2.0.0 cook→implement merge | No action — `cook` here correctly names the skill that existed and was being improved at the time of this report |
| `proofpunk-skills-improvement-report-round2.md` | 17 | `` \| 6 \| marketplace/plugin manifests \| 18 → 19 skills; version 1.8.1 → 1.9.0 \| registration of #5 \| `` | **HISTORICAL PROVENANCE** — before/after row for a specific dated release bump | No action — explicitly a delta row |
| `proofpunk-skills-improvement-report-round2.md` | 33 | `` manifests: zero stale "1.8.1" / "18 skills" strings (grep-verified). `` | **HISTORICAL PROVENANCE** — a verification note for the v1.9.0 release, confirming a sweep was done at that time | No action |
| `tools/INSTALL.md` | 3–4 | `` `tools/proofpunk-install.sh` installs the 18 Proofpunk skills as **plain skills**... `` | 18 (correct) | Hardcoded literal, currently accurate; canonical source same glob |
| `tools/INSTALL.md` | 133 | `` # 1) First-time Claude Code install — 18 skills + doctrine, verified: `` | 18 (correct) | Hardcoded, currently accurate |
| `plugins/proofpunk/.claude-plugin/plugin.json` | 4 | `description` field: `"...18 skills: functional validation, full functional audit, ..."` | **The literal number "18" is correct.** BUT the description enumerates skill capabilities by NAME starting with `"functional validation"` — **this is the dead skill name `functional-validation` appearing as the FIRST capability named in the plugin's own published description string**, which is user-facing marketplace copy, not internal doctrine | **ACTIVE GUIDANCE — must be fixed.** This is the live Claude Code plugin manifest description shown in `/plugin` listings and the marketplace. It should name a currently-existing skill/capability (e.g. "end-user testing" or "implement's inline validation") instead of the phrase "functional validation," which reads as the removed skill's exact former name |
| `plugins/proofpunk/.omp-plugin/plugin.json` | 4 | Identical `description` field, byte-for-byte same string as `.claude-plugin/plugin.json` | Same defect as above | **ACTIVE GUIDANCE — must be fixed, same fix as `.claude-plugin/plugin.json`.** Both manifests should be kept byte-identical per the "version must match" convention noted in `plugins/proofpunk/AGENTS.md:15`, so this is one defect duplicated across two files that must be fixed together |
| `plugins/proofpunk/package.json` | 4 | Identical `description` field, third copy of the same string (`"18 skills: functional validation, full functional audit, ..."`) | Same defect, third occurrence | **ACTIVE GUIDANCE — must be fixed, third copy of the same string.** This is the OMP plugin manifest description; per `AGENTS.md:14` this file's version "must match `.claude-plugin/plugin.json`" — the description strings should be kept in the same sync discipline, all three sharing one canonical source phrase |
| `.claude-plugin/marketplace.json` | 7 | `metadata.description`: `"Execution-first delivery plugin: 18 skills with end-user testing as the only proof..."` | 18 (correct); no dead skill name here (does not say "functional validation") | No action — this description is generic and does not name specific skill capabilities the way the three files above do |
| `.omp-plugin/marketplace.json` | 8 | `metadata.description`: `"Evidence-driven delivery pipeline: 18 skills, 20 flat-black cyberpunk themes..."` | 18 (correct); no dead skill name | No action — same as above |
| `tools/build-site.py` | 162–163 | `` SKILL_DIRS = len([d for d in glob.glob(...) if os.path.isdir(d)]) `` + `` assert len(enforces) == SKILL_DIRS, f"README skill table has {len(enforces)} rows but {SKILL_DIRS} skill dirs exist" `` | **Not a hardcoded literal — a self-checking derivation with a build-time assertion.** This is the canonical pattern the whole rest of the drift table should follow | No action — this is correct-by-construction: `SKILL_DIRS` is `len(glob(...))`, never a literal number, and the script fails loudly (`AssertionError`) if `README.md`'s skill table drifts out of sync with the actual `skills/` directory count. **This is the anti-drift mechanism already in place for the README skill TABLE specifically** — it does NOT cover the prose literals ("18 skills that make...") elsewhere in the same README, which is why those prose literals above are still exposed to drift even though this script exists |
| `tools/build-site.py` | 167–175 | `LAYERS = [("ORCHESTRATION", ["proofpunk", "implement"]), ("PROMPT & PLAN", [...]), ...]` | Hardcoded Python list of skill-name strings, cross-checked below | **Not count drift, but NAME drift risk.** Verified every name in every `LAYERS` tuple against the current 18-skill glob list: `proofpunk, implement, prompt-forge, brainstorm, validation-plan, plan-hardening, stack-testing, mobile-validation-runner, end-user-testing, visual-inspection, ui-experience-audit, full-functional-audit, tui-testing, root-cause-debugging, red-team-eval, production-readiness, session-intent, codebase-truth-audit` — that is **all 18 current skill names, exactly once each, zero mentions of `cook` or `functional-validation`**. **No drift found here.** This confirms the specific concern in the work order ("Strings here reproduce on every site regeneration, which is how a removed command survived its own deletion") does **NOT currently apply to the `LAYERS` map** — it is clean. The concern would apply if a future skill rename/removal is not mirrored into this list, since nothing asserts `LAYERS` membership against the skill glob the way `SKILL_DIRS`/`enforces` are cross-checked at line 163 |
| `tools/build-site.py` | 188, 360 | `` print(f"skills={N_SKILLS} cmds={N_CMDS}+{N_OPCMDS} refs={N_REFS} themes={N_THEMES} docs={N_DOCS} v{VERSION}") `` and the `index.html` chrome description `"...18 skills where end-user testing is the only PASS."` | Line 188 is fully derived (`N_SKILLS = len(skills)`, etc. — no literal). **Line 360 IS a hardcoded literal `"18 skills"` inside an f-string passed to `chrome()`, which becomes the generated `index.html` page's own description meta text** | **ACTIVE GUIDANCE — should be fixed, though currently accurate (18=18).** Line 360's `"18 skills"` should be `f"{N_SKILLS} skills"` since `N_SKILLS` is already computed and in scope two lines above the `write("index.html", ...)` call at line 358 — this is the single highest-leverage fix in the whole table: it is inside the site generator itself, reproduces on every `build-site.py` run, is currently correct only by luck (not by derivation), and is the exact class of bug the work order named ("Strings here reproduce on every site regeneration, which is how a removed command survived its own deletion") — though in this specific instance the surviving string is a NUMBER, not the `cook` command name (see next row for the actual `cook`-adjacent search result) |

## 2. Search for the literal `/proofpunk:cook` reference in `tools/build-site.py`

No occurrence of `cook` (in any form — `/proofpunk:cook`, `` `cook` ``, `cook.md`, etc.) was found anywhere in `tools/build-site.py`. `grep -n cook tools/build-site.py` (via the `grep` tool, `path: tools/build-site.py`, `pattern: cook`) returned **zero matches**. The `LAYERS` map (§1, `build-site.py:167-175`) was independently checked name-by-name against the 18 current skills and also contains no `cook` entry. **The work order's specific concern — a hardcoded `/proofpunk:cook` string surviving in the site generator — is NOT reproducing in the current tree.** Either it was already fixed before this session, or the "dead command survived its own deletion" defect the work order references was a different string (possibly the `"18 skills"` / count-literal class documented at `build-site.py:360` above, which is the closest surviving un-derived literal this session found in that file) rather than a literal occurrence of the word `cook`.

## 3. Summary verdict on the two suspect release-report claims

| Claim (from `proofpunk-hooks-release-report.md`) | Stated | Current true value | Verdict |
|---|---|---|---|
| "Hook events covered: 5" | 5 | **7** | **REFUTED against current HEAD.** Confirmed accurate for the report's own stated snapshot (v1.10.0, 2026-08-13) — `PostToolUse` and `PostToolUseFailure` were added afterward. No file needs editing; the report is a dated historical artifact, not live doctrine, and correctly describes its own point in time. |
| "Commands: 7+7" | 7+7 | **6+6** | **REFUTED against current HEAD.** Confirmed accurate immediately after `install` was added at v1.10.0 (going from a 6+6 baseline to 7+7). The count later returned to 6+6 (verified via glob in §0), meaning some other command was subsequently removed while `install` was kept — net effect is the same total, different membership. No file needs editing for the same historical-snapshot reasoning. |

## Open / UNRESOLVED

- **Edge counts in `architecture.md:123`** ("47 edges total: 17 from the router... plus 30 among the 17 delivery skills") were NOT independently re-derived in this pass — verifying them would require parsing every skill's Related Skills / delegation section and building the full graph, which is out of scope for a count/name drift sweep and was not attempted. Flagged, not confirmed or refuted.
- **Which specific command was removed between the v1.10.0 "7+7" snapshot and today's "6+6"** was not identified by name in this pass — only the net count (6+6) and the fact that `install` survived (it's one of the current 6) were confirmed. Identifying the removed 7th command by name and commit would require walking `git log` on `plugins/proofpunk/commands/` and `opencode/commands/`, which this READ-ONLY, no-`git log`-mandated pass did not perform (the work order did not authorize `git log`; only `bash tools/test-hooks.sh` / `python3 tools/verify-orchestration.py` style read-only gates were pre-approved, and this session ran neither since no code was changed).
- **`plugins/proofpunk/docs/validation-results.md`'s title (`v1.0.0`) vs. its own later-dated internal sections (v1.3.0 at line 288, and unversioned "skill #18" narrative near line 469)** is flagged as a documentation-organization concern, not corrected here since every individual literal inside the file is accurate for its own internal temporal scope — this is a maintainer judgment call (retitle vs. split) outside a mechanical drift fix.
- **`plugins/proofpunk/AGENTS.md:30`'s omission of the OpenCode-only 4th agent (`proofpunk.md`)** is an incompleteness, not a wrong-literal drift instance, so it was noted but not entered as a table row requiring a "true value" correction.
- Exact UTC timestamp at the top of this file is approximate — no live `date` command was run during this session (all evidence came from `read`/`grep`/`glob`/one `bash python3 -c` call); stated as best-effort per the contract's "Measured: <UTC timestamp>" header requirement.
