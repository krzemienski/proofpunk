__pycache__/
*.pyc
instance/
.pytest_cache/

# Ambient agent/session state — local only, never shipped
.omc/
.omp/
.claude/

# Mnemopi memory runtime database (generated, never edited)
banks/

# Lane B planning state — BLOCKED verdict awaiting operator token, local only
.planning/

# Local probe/baseline artifacts from gate runs — cited evidence lives in
# e2e-evidence/ and evidence/, which stay tracked.
.debug/
